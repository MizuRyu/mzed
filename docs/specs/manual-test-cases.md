# 手動テストケース集

リリース前に毎回実施する回帰テスト。TC-ID は永続識別子として変更しない。

> 自動テスト済みの項目は「自動化済み」欄に `tests/instance_integration.rs` 等を明記し、手動実施から外してよい。

---

## セキュリティ（SEC）

### SEC-01 IPC 非 .md ファイル拒否

| 項目 | 内容 |
|---|---|
| **前提** | mzed が起動済み |
| **手順** | 別ターミナルで `mzed /etc/hosts`（非 .md ファイル）を実行する |
| **期待結果** | 既存ウィンドウに変化なし。クラッシュしない |

### SEC-02 IPC ディレクトリ開く

| 項目 | 内容 |
|---|---|
| **前提** | mzed が起動済み |
| **手順** | `mzed ./some-dir`（ディレクトリ）を別ターミナルで実行する |
| **期待結果** | OpenDir としてルート切替が行われる（従来どおり） |

### SEC-03 ソケット権限 0600

| 項目 | 内容 |
|---|---|
| **前提** | mzed が起動済み |
| **手順** | `ls -l "$TMPDIR"mzed-*.sock` を実行する |
| **期待結果** | `srw-------`（0600）が表示される |
| **自動化済み** | `tests/instance_integration.rs` の `ソケット権限は0600に設定される` |

### SEC-04 非 .md 相対リンク無効化

| 項目 | 内容 |
|---|---|
| **前提** | `./page.html` 等の非 .md 相対リンクを含む Markdown ファイルを表示中 |
| **手順** | そのリンクをクリックする |
| **期待結果** | WebView が外部 HTML に遷移しない。何も起きないか、無反応 |

### SEC-05 アンカーリンク正常動作

| 項目 | 内容 |
|---|---|
| **前提** | 見出しへのアンカーリンク（`#section`）を含む Markdown を表示中 |
| **手順** | アンカーリンクをクリックする |
| **期待結果** | ページ内の該当見出しへスクロールする |

### SEC-06 .md 相対リンクとアプリ内遷移

| 項目 | 内容 |
|---|---|
| **前提** | `./other.md` への相対リンクを含む Markdown を表示中 |
| **手順** | リンクをクリックする |
| **期待結果** | アプリ内でそのファイルをタブで開く |

### SEC-07 `.markdown` 拡張子の相対リンクもアプリ内遷移

| 項目 | 内容 |
|---|---|
| **前提** | `./note.markdown` への相対リンクと参照先ファイルが存在するプロジェクトを表示中 |
| **手順** | リンクをクリックする |
| **期待結果** | アプリ内でその `.markdown` ファイルをタブで開く（`.md` と同じ挙動）。大文字 `.MD` / `.Markdown` も同様 |

### SEC-08 `mailto:` リンクが既定メールクライアントで開く

| 項目 | 内容 |
|---|---|
| **前提** | `[連絡](mailto:a@b.com)` を含む Markdown を表示中。設定 `external_links_in_browser` が ON |
| **手順** | リンクをクリックする |
| **期待結果** | OS の既定メールクライアントが新規メール作成で起動する。設定 OFF 時は何も起きない |

---

## Obsidian wikilink（WIKI）

### WIKI-01 `[[target]]` で同ディレクトリのファイルへ遷移

| 項目 | 内容 |
|---|---|
| **前提** | `docs/a.md` と `docs/b.md` が存在するプロジェクトを開いている |
| **手順** | `a.md` に `[[b]]` と書き、保存後に mzed でレンダリング結果を表示。`[[b]]` のリンクをクリックする |
| **期待結果** | `b.md` がアプリ内で開く（通常の `.md` リンクと同じ遷移） |

### WIKI-02 `[[target|alias]]` で表示テキストが alias になる

| 項目 | 内容 |
|---|---|
| **前提** | `notes.md` が存在するプロジェクト |
| **手順** | `[[notes|My Notes]]` を含む Markdown を表示する |
| **期待結果** | リンクの表示テキストが「My Notes」になっている。クリックで `notes.md` が開く |

### WIKI-03 `[[target#Heading]]` でファイル遷移する

| 項目 | 内容 |
|---|---|
| **前提** | `guide.md` が存在し、見出し `## Setup` を含む |
| **手順** | `[[guide#Setup]]` を含む Markdown を表示。リンクをクリックする |
| **期待結果** | `guide.md` が開く（v1 ではアンカースクロールは保証しない） |

### WIKI-04 未解決 wikilink がミュート表示になる

| 項目 | 内容 |
|---|---|
| **前提** | 存在しないファイル名を参照する wikilink を含む Markdown を表示 |
| **手順** | `[[nonexistent-page]]` を含む Markdown を表示する |
| **期待結果** | テキストがミュートカラー（灰色）＋点線下線で表示される。クリックしても遷移しない |

### WIKI-05 非画像 `![[...]]` embed はプレーンテキストのまま

| 項目 | 内容 |
|---|---|
| **前提** | 任意の Markdown ファイル |
| **手順** | `![[note.md]]` を含む Markdown を表示する |
| **期待結果** | リンク化・画像化されず、`![[note.md]]` がそのままテキストとして表示される（画像以外の埋め込みは対象外） |

### WIKI-06 サブディレクトリのファイルをファイル名で解決する

| 項目 | 内容 |
|---|---|
| **前提** | `docs/sub/target.md` が存在するプロジェクト。ルートから表示中の Markdown に `[[target]]` |
| **手順** | `[[target]]` を含む Markdown を表示してリンクをクリックする |
| **期待結果** | `docs/sub/target.md` が開く（basename 検索で解決） |

### WIKI-07 wikilink が XSS を引き起こさない

| 項目 | 内容 |
|---|---|
| **前提** | 任意の Markdown ファイル |
| **手順** | `[[<script>alert(1)</script>]]` を含む Markdown を表示する |
| **期待結果** | スクリプトが実行されない。エスケープされたテキストまたは未解決リンクとして表示される |

---

## 画像埋め込み（IMG）

### IMG-01 ローカル SVG が表示される

| 項目 | 内容 |
|---|---|
| **前提** | roots 配下に `icon.svg`（8MB 以内）が存在 |
| **手順** | `![](./icon.svg)` を含む Markdown を表示する |
| **期待結果** | SVG が画像として表示される（data URL 埋め込み）。クリックで lightbox が開く。SVG 内スクリプトは実行されない |

### IMG-02 `![[image.png]]` 埋め込みが表示される

| 項目 | 内容 |
|---|---|
| **前提** | roots 配下に `pic.png` が存在 |
| **手順** | `![[pic.png]]` を含む Markdown を表示する |
| **期待結果** | 画像が表示される。`![[...]]` のリテラル文字は残らない |

### IMG-03 `![[image.png|400]]` で表示幅が指定される

| 項目 | 内容 |
|---|---|
| **前提** | roots 配下に `pic.png`（実寸 400px 超）が存在 |
| **手順** | `![[pic.png\|400]]` を含む Markdown を表示する |
| **期待結果** | 画像が幅 400px で表示される。`mdo-width-400` 等のマーカー文字は表示されない |

### IMG-04 `![[image.png|説明]]` で alt テキストになる

| 項目 | 内容 |
|---|---|
| **前提** | roots 配下に `pic.png` が存在 |
| **手順** | `![[pic.png\|図の説明]]` を含む Markdown を表示する |
| **期待結果** | 画像が表示され、alt に「図の説明」が入る（数値でないため幅ではなく alt 扱い） |

### IMG-05 未解決の画像埋め込みがミュート表示になる

| 項目 | 内容 |
|---|---|
| **前提** | 参照先が存在しない |
| **手順** | `![[missing.png]]` を含む Markdown を表示する |
| **期待結果** | 画像化されず、ファイル名がミュートカラー＋点線下線（未解決 wikilink と同じ）で表示される |

### IMG-06 roots 外の画像は埋め込まれない

| 項目 | 内容 |
|---|---|
| **前提** | roots の外に `secret.png` が存在 |
| **手順** | `![[../secret.png]]` または `![](../../secret.png)` を含む Markdown を表示する |
| **期待結果** | 画像は表示されない（`src` 削除／未解決 demote）。roots 外のファイルは読み込まれない |

---

## 生 HTML 許可サブセット（HTML）

allowlist 方式で再構築する安全な生 HTML サブセット（`<img>` / `<p align>` / `<kbd>` 等）の回帰。詳細は `docs/specs/06-rendering-pipeline.md` の「生 HTML 許可サブセット」を参照。

### HTML-01 `<p align="center"><img>` が中央寄せで表示される

| 項目 | 内容 |
|---|---|
| **前提** | roots 配下に `icon.png` が存在 |
| **手順** | `<p align="center"><img src="icon.png" width="128"></p>` を含む Markdown を表示する |
| **期待結果** | 画像が中央寄せ・幅 128px で表示される（Markdown 画像と同じ data URL 埋め込み・クリックで lightbox）。生タグのリテラル文字は残らない |

### HTML-02 `<kbd>` / `<sub>` / `<sup>` / `<br>` が描画される

| 項目 | 内容 |
|---|---|
| **前提** | なし |
| **手順** | `Press <kbd>Ctrl</kbd>+<kbd>C</kbd><br>H<sub>2</sub>O、x<sup>2</sup>` を含む Markdown を表示する |
| **期待結果** | キー表示・下付き・上付き・改行が描画される（エスケープ文字のまま残らない） |

### HTML-03 `<img onerror>` / 未許可属性が無害化される

| 項目 | 内容 |
|---|---|
| **前提** | なし |
| **手順** | `<img src=x onerror=alert(1)>` と `<p align="center" style="x" onclick="y">t</p>` を含む Markdown を表示する |
| **期待結果** | `onerror` / `style` / `onclick` が DOM に一切現れない。アラートは発火しない。align のみ残る |

### HTML-04 未許可タグ・危険 src はエスケープ表示のまま

| 項目 | 内容 |
|---|---|
| **前提** | なし |
| **手順** | `<script>alert(1)</script>`、`<iframe src=x>`、`<img src="javascript:alert(1)">`、`<img src="/etc/passwd">` を含む Markdown を表示する |
| **期待結果** | script / iframe / javascript: 画像はリテラルテキスト（エスケープ表示）のまま。実行・遷移・ファイル読み込みは起きない。roots 外/絶対パス画像は表示されない |

### HTML-05 `<details>`/`<summary>` の折り畳みが機能する

| 項目 | 内容 |
|---|---|
| **前提** | なし |
| **手順** | `<details open><summary>見出し</summary>本文</details>` を含む Markdown を表示する |
| **期待結果** | 初期状態で開いた折り畳みが描画され、クリックで開閉できる |

---

## エクスポート（EXP）

### EXP-01 HTML エクスポート連番

| 項目 | 内容 |
|---|---|
| **前提** | エクスポート先に `title.html` が既に存在する |
| **手順** | 同じファイルを HTML エクスポートを2回実行する（1回目はファイルなし前提で） |
| **期待結果** | `title.html` と `title (1).html` の両方が出力される |

### EXP-02 HTML 自己完結確認

| 項目 | 内容 |
|---|---|
| **前提** | 画像・コードブロック・Mermaid を含む Markdown を表示中 |
| **手順** | HTML エクスポートし、出力ファイルを Safari / Chrome でネットワーク切断状態で開く |
| **期待結果** | 画像・シンタックスハイライトが表示される |

### EXP-03 PDF エクスポートキャンセル

| 項目 | 内容 |
|---|---|
| **前提** | Markdown ファイルを表示中 |
| **手順** | PDF エクスポートを実行し、印刷ダイアログでキャンセルを押す |
| **期待結果** | アプリが正常に動作し続ける |

### EXP-04 ズーム後の HTML エクスポート

| 項目 | 内容 |
|---|---|
| **前提** | mermaid 図を含む Markdown を表示中 |
| **手順** | インライン図を ⌘+スクロールで拡大しドラッグで移動してから、HTML エクスポートする |
| **期待結果** | 出力 HTML の図は等倍・中央で、欠けていない（ライブ表示側の拡大はそのまま残る） |
| **自動化済み** | `src/js.rs` の `export_capture_js_clears_inline_zoom_transform` |

---

## プロジェクト連動（ZED / ORC）

### ZED-01 Zed 未起動時の起動

| 項目 | 内容 |
|---|---|
| **前提** | Zed が完全終了している |
| **手順** | mzed を起動する |
| **期待結果** | クラッシュなし。空表示または前回セッションが表示される |

### ZED-02 高速プロジェクト切替

| 項目 | 内容 |
|---|---|
| **前提** | Zed と mzed が起動中（sync: auto） |
| **手順** | Zed でプロジェクトを素早く連続切替（数回） |
| **期待結果** | mzed が最後のプロジェクトで安定する。古い結果が後から上書きしない |

### ZED-03 self モードでタブが奪われないこと

| 項目 | 内容 |
|---|---|
| **前提** | sync モードを self に設定。mzed で md を 1 つ以上開いておく（分割表示も開いておくとなおよい） |
| **手順** | Zed で別プロジェクトにフォーカスする |
| **期待結果** | サイドバーの root は切り替わるが、**開いているタブ・アクティブタブ・分割はそのまま**。md は自動で開かない |
| **備考** | 仕様は「サイドバーは更新、ビューアは現状維持」（→ [05](05-zed-integration.md)）。Orca 由来の切替も同じ |

### ZED-04 マルチルートワークスペース

| 項目 | 内容 |
|---|---|
| **前提** | Zed に複数フォルダを追加したマルチルートワークスペースが存在する |
| **手順** | そのワークスペースに Zed でフォーカスする |
| **期待結果** | mzed のサイドバーに全ルートのファイルが表示される |

### ZED-05 同一プロジェクト再フォーカスで再ロードしない

| 項目 | 内容 |
|---|---|
| **前提** | mzed で特定ファイルを開いた状態。Zed が同じプロジェクトにフォーカス中 |
| **手順** | Zed で別ウィンドウを開いた後、同じプロジェクトに戻ってフォーカスする |
| **期待結果** | mzed の表示が変化しない（タブが再ロードされない、スクロール位置が保たれる） |

### ZED-06 Zed で worktree を開いたときの 3 択

| 項目 | 内容 |
|---|---|
| **前提** | あるリポで `git worktree add` した worktree が存在。mzed は別のプロジェクトを表示中（sync: auto） |
| **手順** | 設定 → プロジェクト連動 → 「worktree を開いたとき」を 1. 親リポジトリに切り替える（既定） 2. 無視する 3. そのまま開く の順に変え、それぞれで Zed からその worktree を開く |
| **期待結果** | 1. mzed が **main の checkout** に切り替わり、サイドバーに worktree 側の md も合成されている。2. 表示が変わらない。3. worktree 自体が開く |
| **備考** | 判定は `.git` がファイルかどうか（submodule checkout も同様）。親が引けない通常の checkout は 1. でも付け替わらない |
| **自動化済み** | `src/files.rs` の `is_git_worktreeはgitファイルのみ真`、`src/worktrees.rs` の `redirect` 系 4 テスト、`src/sync.rs` の `worktreeイベントを落とすのはZedのskipだけ` |

### ZED-09 明示的に開いた worktree も親に集約される

| 項目 | 内容 |
|---|---|
| **前提** | `worktree_switch`: `main`（既定）。linked worktree のパスが分かっている |
| **手順** | 1. `mzed <worktree のパス>` を実行。2. Cmd+O からその worktree を選ぶ（無ければ「フォルダを開く」で選択）。3. worktree のフォルダをウィンドウにドラッグ&ドロップ。4. お気に入りに登録した worktree の行をクリック |
| **期待結果** | どの経路でも main の checkout が開く（ヘッダのプロジェクト名が main のフォルダ名になる） |
| **備考** | `skip` では明示操作を制限しない（worktree がそのまま開く）。`skip` は Zed 由来の切替だけに効く |

### ZED-10 旧 `sync_skip_worktrees` が移行される

| 項目 | 内容 |
|---|---|
| **前提** | `~/.config/mzed/config.json` に `"sync_skip_worktrees": true`（または `false`）があり、`worktree_switch` が無い |
| **手順** | 1. mzed を起動し、数秒後に config.json を見る。2. `mzed serve <dir>` だけを実行して config.json の更新時刻を見る |
| **期待結果** | 1. `worktree_switch` が `"skip"`（`false` なら `"follow"`）になり、`sync_skip_worktrees` が消えている。設定画面の「worktree を開いたとき」も同じ値を表示する。2. config.json は書き換わらない（読み込みは書き込まない） |
| **自動化済み** | `src/config.rs` の `旧sync_skip_worktreesはworktree_switchへ移行する` ほか 2 テスト |

### ZED-11 同じリポジトリの worktree 間を移ってもタブが変わらない

| 項目 | 内容 |
|---|---|
| **前提** | `worktree_switch`: `main`（既定）。1 つのリポジトリに worktree が 2 つ以上。mzed はその main を表示中で、代表 Markdown 以外のファイルをアクティブタブにし、フォルダをいくつか開いておく |
| **手順** | 1. Zed / Orca で worktree A に移る。2. 続けて worktree B に移る。3. main 自体に戻る |
| **期待結果** | いずれもアクティブタブ・タブの並び・ツリーの展開状態・スクロール位置が変わらない（どれも親から見れば同じプロジェクト。代表 Markdown が開き直されない） |
| **備考** | 壊れた worktree 登録（`gitdir` の指す先が消えている）を開いた場合は付け替えず、その worktree がそのまま開く（`/tmp` などの無関係なディレクトリに飛ばない） |
| **自動化済み** | `src/worktrees.rs` の `redirectは同じ親の別worktreeを同じ選択に写す` / `main_root_ofは壊れたgitdirを親と誤認しない`、`src/app.rs` の `same_selection_needs_the_whole_root_set` |

### ZED-07 worktree オーバーレイ: 本文が最新の checkout を映す

| 項目 | 内容 |
|---|---|
| **前提** | main checkout を mzed で表示中。同リポの linked worktree が存在し、両方に同じ md がある |
| **手順** | 1. worktree 側でその md を編集・保存する。2. main 側で同じ md をさらに編集・保存する |
| **期待結果** | 1. mzed の表示が worktree 側の内容に即切り替わる（フォルダ移動・タブ操作なし）。2. main 側の内容に戻る（常に mtime が新しい方） |
| **備考** | 表示だけのオーバーレイ。ファイルのコピー・書き込みはしない。出所バッジは出さない仕様 |
| **自動化済み** | `src/worktrees.rs` の `resolveはmtimeが新しい側を選ぶ` |

### ZED-08 worktree オーバーレイ: worktree にしかないファイルがツリーに出る

| 項目 | 内容 |
|---|---|
| **前提** | main checkout を表示中。worktree 側にだけ存在する md がある（新規作成） |
| **手順** | 1. サイドバーを確認する。2. そのファイルを開く。3. Task View（worktree にだけあるタスクフォルダ）を確認する |
| **期待結果** | 1. main のツリーの対応する位置に表示される。2. worktree 側の内容が表示される。3. main プロジェクトのタスクとして表示される。両 checkout に同名タスクがある場合は task.md が新しい側だけ残る |
| **備考** | ツリー・タブ・セッションのパスは main 側の論理パス。worktree を削除するとツリーからも消える |
| **自動化済み** | `src/files.rs` の `build_tree_overlayはworktree限定のmdを主パスで合成する`、`src/services/task_scan.rs` の worktree 系 3 テスト |

### ORC-01 Orca の worktree 切替に追従する

| 項目 | 内容 |
|---|---|
| **前提** | Orca と mzed が起動中（sync: auto、sync_source: auto） |
| **手順** | Orca で別の worktree に切り替える |
| **期待結果** | 1.5 秒以内に mzed のプロジェクトが切り替わり、代表 Markdown が開く |
| **備考** | 状態は `~/Library/Application Support/orca/profiles/<profile>/orca-data.json`（Orca の未文書化内部ファイル） |

### ORC-02 Orca の worktree 切替も親に集約される（`skip` の対象外）

| 項目 | 内容 |
|---|---|
| **前提** | Orca に linked worktree（`.git` がファイル）の workspace がある |
| **手順** | 「worktree を開いたとき」を 1. 親リポジトリに切り替える（既定） 2. 無視する の順に変え、それぞれ Orca でその worktree に切り替える |
| **期待結果** | 1. mzed が main の checkout に切り替わり、サイドバーに worktree 側の md も合成されている。2. **worktree 自体に切り替わる**（`skip` は Zed 由来だけに効く） |
| **備考** | Orca は worktree 管理アプリなので、スキップすると連動そのものが成立しない |

### ORC-03 追従元の切り替え

| 項目 | 内容 |
|---|---|
| **前提** | Zed と Orca が別々のプロジェクトを開いている |
| **手順** | 設定 → プロジェクト連動 → 追従元を 1. Zed 2. Orca 3. 自動 の順に変え、それぞれで Zed 側・Orca 側を切り替える |
| **期待結果** | 1. Orca の切替に反応しない。2. Zed の切替に反応しない。3. どちらの切替にも、操作した順に追従する |
| **備考** | 稼働中に溜まったイベントは到着順で最後のものに畳み込まれ、ソースによる優先は無い |

### ORC-07 起動時は Zed のプロジェクトに着地する

| 項目 | 内容 |
|---|---|
| **前提** | `sync_source: auto`。Zed と Orca が別々のプロジェクトを開いている |
| **手順** | mzed を起動する |
| **期待結果** | Zed 側のプロジェクトが開く（起動時の報告は操作ではないので到着順で決めない） |
| **備考** | Zed の DB（`~/Library/Application Support/Zed/db/0-stable/db.sqlite`）が無い場合は Orca 側に着地する（zed.rs は Zed のプロセスではなく DB の有無しか見ない）。起動後にユーザーが切り替えたら、遅れて届いた初回報告はそれを巻き戻さない |
| **自動化済み** | `src/sync.rs` の `起動報告はZedが勝つ` / `Zedの起動報告が無ければOrcaに着地する` / `遅れて来た起動報告は実際の切替を上書きしない` |
| **自動化済み** | `src/sync.rs` の `固定ソースは他方を拒否する` / `autoは両方のソースを受け入れる` / `到着順で最後のイベントを採る` / `ソースによる優先はない` / `プロジェクト無しのイベントは後続でも採らない` |

### ORC-04 sync_mode との組み合わせ

| 項目 | 内容 |
|---|---|
| **前提** | sync_source: auto または orca |
| **前提** | sync_source: auto または orca。mzed で md を 1 つ以上開いておく |
| **手順** | sync_mode を 1. off 2. self にして Orca の worktree を切り替える |
| **期待結果** | 1. mzed は何も変わらない。2. サイドバーの root だけ切り替わり、開いているタブ・分割はそのまま |

### ORC-05 Orca の状態ファイルが消えても落ちない

| 項目 | 内容 |
|---|---|
| **前提** | mzed 起動中（sync_source: auto または orca） |
| **手順** | `orca-data.json` を一時的にリネームして数秒待ち、元に戻してから Orca で worktree を切り替える |
| **期待結果** | mzed は落ちず表示も変わらない。戻した後は追従が再開する |
| **備考** | Orca 未インストールの環境でも同様に何も起きない。状態は `~/Library/Logs/mzed/mzed.log` の `orca:` 行で確認できる（同じ行は繰り返さない） |
| **自動化済み** | `src/orca.rs` の `ファイルが無くてもパニックしない` / `壊れたJSONは前回値を維持し次回再読込する` / `キー欠落は前回値を維持し再パースもしない` |

### ORC-06 mzed 起動後に Orca を入れても追従が始まる

| 項目 | 内容 |
|---|---|
| **前提** | Orca の状態ファイルがまだ無い状態で mzed を起動する |
| **手順** | mzed を起動したまま Orca を初回起動し、worktree を開く |
| **期待結果** | mzed を再起動せずに追従が始まる。ログに `orca: no state file found` の後 `orca: reading ...` が出る。**Zed のプロジェクトに着地済みなら、この時点で表示は動かない**（Orca の初回報告は起動時の報告であり、ユーザー操作ではないため）。次に Orca で worktree を切り替えると追従する |
| **自動化済み** | `src/orca.rs` の `後から現れた状態ファイルを拾う` |
---

## マルチウィンドウ（WIN）

### WIN-01 2枚目ウィンドウは SelfPinned で起動

| 項目 | 内容 |
|---|---|
| **前提** | mzed のベースウィンドウが起動中（sync: auto） |
| **手順** | Cmd+N で新規ウィンドウを開く |
| **期待結果** | 新規ウィンドウのツールバー/パレットで sync モードが SelfPinned になっている |

### WIN-02 ベースウィンドウのみ Zed 追従

| 項目 | 内容 |
|---|---|
| **前提** | ベースウィンドウと2枚目ウィンドウが起動中。2枚目は別プロジェクトA を表示 |
| **手順** | Zed で別プロジェクトB に切替える |
| **期待結果** | ベースウィンドウがプロジェクトB に切替わる。2枚目ウィンドウはプロジェクトA のまま変化しない |

### WIN-03 シングルインスタンス（2重起動）

| 項目 | 内容 |
|---|---|
| **前提** | mzed が起動中 |
| **手順** | `mzed some.md` を別ターミナルで実行する |
| **期待結果** | 2つ目のプロセスは起動しない。既存ウィンドウの新規タブで `some.md` が開く |
| **自動化済み** | `tests/instance_integration.rs` の `二重起動は既存インスタンスにルーティングされる` |

### WIN-04 stale socket 回収

| 項目 | 内容 |
|---|---|
| **前提** | mzed を `kill -9` で強制終了する |
| **手順** | mzed を再起動する |
| **期待結果** | stale socket が回収され、正常に起動する |
| **自動化済み** | `tests/instance_integration.rs` の `stale_socketは回収される` |

### WIN-05 最後のタブを閉じるとウィンドウが消える（プロセスは残る）

| 項目 | 内容 |
|---|---|
| **前提** | ベースウィンドウにタブが1つだけ開いている |
| **手順** | Cmd+W を押す |
| **期待結果** | ウィンドウが画面から消える。アプリは終了せず Dock に残る（`ps` でプロセスが生きている）。タブが 0 枚のウィンドウで Cmd+W を押しても同じ |
| **自動化済み** | `src/app.rs` の `closing_the_last_tab_empties_the_window`（判定のみ。hide 自体は目視） |

### WIN-06 消えたウィンドウは IPC で戻る

| 項目 | 内容 |
|---|---|
| **前提** | WIN-05 の状態（ウィンドウが消えている） |
| **手順** | 別ターミナルで `mzed some.md` を実行する |
| **期待結果** | 同じプロセスのウィンドウが画面に戻り、`some.md` のタブだけが開いている（閉じたタブは復活しない）。Dock アイコンのクリックでも戻る（このときはタブ無しの空表示で、`state.json` の `tabs` は閉じる直前のまま = 見ただけではタブを失わない） |

### WIN-07 消えたウィンドウは Zed / Orca の切替で戻る

| 項目 | 内容 |
|---|---|
| **前提** | WIN-05 の状態（sync: auto）。実機確認は HOME + TMPDIR 分離 + `MZED_ORCA_DATA` |
| **手順** | Zed / Orca で別プロジェクトに切替える |
| **期待結果** | ウィンドウが画面に戻り、切替先プロジェクトの md が開く。いま表示中と同じプロジェクトが再報告されただけでは戻らない |

### WIN-08 2枚目ウィンドウのタブを全部閉じても1枚目は残る

| 項目 | 内容 |
|---|---|
| **前提** | ベースウィンドウと Cmd+N の2枚目が開いている |
| **手順** | 2枚目で最後のタブを Cmd+W で閉じる |
| **期待結果** | 2枚目だけが閉じ、ベースウィンドウは表示されたまま。アプリは終了しない |

### WIN-09 消える直前のタブが次回起動で復元される

| 項目 | 内容 |
|---|---|
| **前提** | タブを1つ開いた状態で Cmd+W → ウィンドウが消えている |
| **手順** | Cmd+Q で終了し、mzed を再起動する |
| **期待結果** | 閉じる直前に開いていたタブが復元される（`state.json` の `tabs` が空にならない）。Dock クリックでウィンドウを空表示のまま戻して眺めただけでも保存は凍結されたまま = タブは失われない。ファイルを開くかプロジェクトを切替えた時点で凍結が解け、以後は通常どおり現在の状態が保存される |

### WIN-10 分割中に左ペインを空にしても退場しない

| 項目 | 内容 |
|---|---|
| **前提** | Cmd+\ で分割し、左ペインに1つ、右ペインに1つタブがある |
| **手順** | 左ペインをフォーカスして Cmd+W を押す |
| **期待結果** | ウィンドウは消えない。分割は畳まれず、左ペインが空表示になる（畳むのは右ペインが空になったときだけ）。続けて右ペインの最後のタブを Cmd+W で閉じるとウィンドウが退場し、`state.json` には**右ペインに残っていたタブ**が保存される |
| **自動化済み** | `src/app.rs` の `a_dismissed_window_persists_the_pane_that_still_has_tabs`（保存対象の選択のみ） |

---

## セッション・設定（SES）

### SES-01 プロジェクト別最終ページ復元

| 項目 | 内容 |
|---|---|
| **前提** | プロジェクトA で `a.md` を開いた後、プロジェクトB に切替えて `b.md` を開く。mzed を終了する |
| **手順** | mzed を再起動し、プロジェクトA に切替える |
| **期待結果** | `a.md` が復元されてアクティブになっている |

### SES-02 死にタブを除外して起動

| 項目 | 内容 |
|---|---|
| **前提** | タブに残っているファイルを Finder で削除してから mzed を終了する |
| **手順** | mzed を再起動する |
| **期待結果** | 削除されたファイルのタブは復元されない |

### SES-03 config 破損耐性

| 項目 | 内容 |
|---|---|
| **前提** | `~/.config/mzed/config.json` を手動で壊す（例: 不正 JSON にする） |
| **手順** | mzed を起動する |
| **期待結果** | クラッシュせずデフォルト設定で起動する |

### SES-04 ショートカット編集の永続化

| 項目 | 内容 |
|---|---|
| **前提** | 設定画面の Hotkeys でショートカットを変更する |
| **手順** | mzed を再起動する |
| **期待結果** | 変更したショートカットが再起動後も反映されている |

### SES-05 ウィンドウ位置の記憶と復元

| 項目 | 内容 |
|---|---|
| **前提** | mzed が起動しており、ウィンドウが画面の任意の位置にある |
| **手順** | 1. ウィンドウをドラッグして画面の右下付近に移動する。2. Cmd+Q で完全終了する。3. mzed を再起動する |
| **期待結果** | 再起動後、ウィンドウが前回終了時と同じ位置・同じサイズで開く。`config.json` に `window_x` / `window_y` が保存されている |

### SES-06 外部ディスプレイ切断後のオフスクリーンフォールバック

| 項目 | 内容 |
|---|---|
| **前提** | 外部ディスプレイを接続し、mzed のウィンドウをその外部ディスプレイ上に移動する。Cmd+Q で終了する |
| **手順** | 1. 外部ディスプレイを取り外す（ノート本体の画面のみ）。2. mzed を再起動する |
| **期待結果** | ウィンドウが画面外に消えず、本体ディスプレイ内の表示可能な位置に自動配置される（OS 既定位置 or 主モニタ付近）。操作不能にならない |
| **自動化** | オフスクリーン判定は `services::platform::clamp_to_monitors` のユニットテストで担保済み |

### SES-07 プロジェクトの別名で切替メニューを検索する

| 項目 | 内容 |
|---|---|
| **前提** | Zed で一度も開いていないフォルダを1つ用意する |
| **手順** | 1. 設定 → 一般 → 「プロジェクトの別名」で「+ フォルダを追加」からそのフォルダを選び、別名を入力する。2. Cmd+O を押し、別名の一部を入力する。3. 別名を削除して再度 Cmd+O を押す |
| **期待結果** | 2. そのフォルダが候補に出る（Zed の履歴に無くても出る）。行にはフォルダ名の隣に別名バッジが付く。Enter で開ける。3. 別名で検索してもヒットしなくなる |
| **備考** | `project_aliases`。既存のパス検索は従来どおり効く |
| **自動化済み** | `src/config.rs` の `alias_forは登録済みパスの別名だけ返す` |

### SES-08 プロジェクト切替メニューから候補を隠す

| 項目 | 内容 |
|---|---|
| **前提** | Cmd+O の候補に複数プロジェクトがある |
| **手順** | 1. 候補行にマウスを乗せて右端の ✕ を押す。2. 設定 → 一般 → 「非表示のプロジェクト」でその行の ✕ を押す。3. 再度 Cmd+O を開く |
| **期待結果** | 1. その候補が即座に消える（メニューは開いたまま。現在のプロジェクト行には ✕ が出ない）。2. リストから消える。3. 候補に戻っている |
| **備考** | `project_menu_hidden`。Zed の履歴は変更せずローカルで隠すだけ |

### SES-09 frontmatter の Metadata を既定で閉じる

| 項目 | 内容 |
|---|---|
| **前提** | YAML frontmatter 付きの md を表示中 |
| **手順** | 1. 表示を確認する。2. 設定 → 一般 → 「Metadata（frontmatter）を開いた状態で表示」を ON にする |
| **期待結果** | 1. Metadata が折りたたまれた状態で表示される（クリックで展開できる）。2. 表示中のドキュメントが即座に展開状態に切り替わる |
| **備考** | `frontmatter_default_open`（既定 false）。`mzed serve` はサーバ起動時の設定値を使う |
| **自動化済み** | `src/markdown/frontmatter.rs` の `open指定でのみdetailsが展開状態になる` |

---

## sync 固定トグル（SYN）

### SYN-01 Cmd+Shift+L で auto⇄self 切替

| 項目 | 内容 |
|---|---|
| **前提** | ベースウィンドウが起動中（sync: auto） |
| **手順** | Cmd+Shift+L を押す |
| **期待結果** | sync モードが SelfPinned に切替わる。トーストが表示される |

### SYN-02 再度 Cmd+Shift+L で auto に戻る

| 項目 | 内容 |
|---|---|
| **前提** | sync モードが SelfPinned |
| **手順** | Cmd+Shift+L を押す |
| **期待結果** | sync モードが Auto に切替わる。トーストが表示される |

### SYN-03 コマンドパレットから Toggle Sync Pin

| 項目 | 内容 |
|---|---|
| **前提** | mzed が起動中 |
| **手順** | Cmd+Shift+P を押し、"sync pin" で絞り込み、Toggle Sync Pin を実行する |
| **期待結果** | sync モードが auto⇄self で切り替わる。トーストが表示される |

---

## ファイル操作・レンダリング（FILE）

### FILE-01 表示中ファイルの外部削除

| 項目 | 内容 |
|---|---|
| **前提** | .md ファイルを表示中 |
| **手順** | そのファイルを Finder または外部エディタで削除する |
| **期待結果** | クラッシュしない |

### FILE-02 ライブリロード連続保存

| 項目 | 内容 |
|---|---|
| **前提** | .md ファイルを外部エディタで開いている |
| **手順** | 数秒間隔で5回程度保存する |
| **期待結果** | 最終状態が正しく描画される。CPU 暴走しない |

### FILE-03 mermaid 構文エラー

| 項目 | 内容 |
|---|---|
| **前提** | mermaid 構文エラーを含む .md を表示 |
| **手順** | ファイルを開く |
| **期待結果** | エラーの図だけがソースのまま表示される（mermaid のエラー画像は出さない）。他の内容は正常に描画される |

### FILE-04 BOM 付きファイルの frontmatter / 見出し

| 項目 | 内容 |
|---|---|
| **前提** | 先頭に UTF-8 BOM を持ち、`---` frontmatter と見出しを含む .md（例: 一部エディタが付与） |
| **手順** | ファイルを開く |
| **期待結果** | frontmatter が「Metadata」折りたたみで表示され、本文先頭に BOM 由来の壊れた見出しが出ない。見出しアンカーも正常 |

### FILE-05 CRLF 改行ファイルの frontmatter / Alerts

| 項目 | 内容 |
|---|---|
| **前提** | Windows 由来の `\r\n` 改行で `---` frontmatter と `> [!WARNING]` を含む .md |
| **手順** | ファイルを開く |
| **期待結果** | frontmatter が折りたたみ表示され、余分な水平線や `title: T` の生見出しが出ない。GitHub Alert も正しくスタイルされる |

### FILE-06 ファイルのドラッグ & ドロップ

| 項目 | 内容 |
|---|---|
| **前提** | プロジェクトを開いた状態。プロジェクト外に相対画像付きの .md がある |
| **手順** | (1) プロジェクト外の .md（`.markdown` や大文字拡張子も）を Finder からウィンドウ上へドラッグし、離す前に画面を見る → そのままドロップ。(2) 画像や zip 等の非 Markdown ファイルをドロップ。(3) ディレクトリをドロップ |
| **期待結果** | (1) ドラッグ中は画面全体にドロップオーバーレイ（点線枠＋「ドロップして開く」）が出る。ドロップするとフォーカス中ペインの新規タブで開き、相対画像・リンクも描画される。**サイドバーのプロジェクトツリーは変化しない**（単体ファイル表示。Task View / ファイル監視のスコープも現プロジェクトのまま）。(2) 「Markdown 以外は開けません」toast が出て何も開かない。(3) そのディレクトリにプロジェクトが切り替わる |
| **備考** | ドロップはウィンドウ全体（サイドバー・本文どこでも）で受け付ける。単体ファイルの親ディレクトリは描画専用 root（`loose_roots`）として保持され、サイドバー・監視・Task View・パレットには一切影響しない。セッション保存もされない |

### FILE-07 symlink されたディレクトリ / md がサイドバーに出る

| 項目 | 内容 |
|---|---|
| **前提** | プロジェクト内に、正本が別の場所にあるディレクトリ symlink（配下に md あり）とファイル symlink（`.md`）がある |
| **手順** | プロジェクトを開き、symlink のフォルダを展開して md を開く |
| **期待結果** | symlink のフォルダ・md がツリーに表示され、クリックで正本の内容が描画される |
| **備考** | symlink 先が roots 外の場合、その md 内の相対画像・`.md` リンクは roots 包含チェックで拒否される（セキュリティ仕様。必要なら正本側をプロジェクト/追加ルートとして開く） |
| **自動化済み** | `src/files.rs` の `symlinkのディレクトリとmdもツリーに載る` |

### FILE-08 綴りの違うパスでもライブリロードが効く

| 項目 | 内容 |
|---|---|
| **前提** | 次のいずれかで開いたプロジェクト: (a) symlink 経由のディレクトリ、(b) `/tmp` 配下（実体は `/private/tmp`）、(c) ホームディレクトリを実際と違う大小文字で綴ったパス |
| **手順** | 1. md を開いた状態で、その md をエディタから編集・保存する。2. 同じプロジェクトに新しい md を追加する |
| **期待結果** | 1. 表示がスクロール位置を保ったまま更新される。2. サイドバーツリーに新しい md が現れる |
| **備考** | FSEvents は完全解決済みパス（symlink 展開・firmlink 展開・ディスク上の大小文字）で通知するため、verbatim 比較だと**エラーも出さずに更新が止まる**。判定は canonicalize + 大小文字無視（→ [06](06-rendering-pipeline.md)） |
| **自動化済み** | `src/watcher.rs` の `symlink越しのアクティブファイルも再読込対象` / `大文字小文字違いのアクティブファイルも再読込対象` / `symlink越しのrootでもツリー更新対象` |

### FILE-09 操作の失敗がログに残る

| 項目 | 内容 |
|---|---|
| **前提** | mzed.app（GUI 起動。stdout/stderr は破棄される） |
| **手順** | 1. サイドバー行を右クリック →「パスをコピー」。2. `tail ~/Library/Logs/mzed/mzed.log` を確認する |
| **期待結果** | `clipboard: copied <絶対パス>` が記録されている。失敗した場合は `clipboard: write failed ...` が残る。UI イベント内の panic は `PANIC at <file>:<line>: ...` として記録される |
| **備考** | 5MB 超で `mzed.log.old` へ1世代ローテート。コピーが効かない等の「押しても何も起きない」系の切り分けはこのログが起点 |

### FILE-10 保存ではサイドバーが再描画されない / 構造変更では即反映される

| 項目 | 内容 |
|---|---|
| **前提** | サブフォルダを持つプロジェクトを開き、その中の md を表示している |
| **手順** | 1. 表示中の md をエディタで数回保存する。2. サイドバーの既存フォルダに新しい md を作る。3. **アプリ起動後に**新しいサブフォルダを作り、その中に md を作る。4. md をリネーム・削除する。5. フォルダを作る／削除する。6. `node_modules/` や `.git/` に md を作る。7. md を含む `docs.v1/` を `target/docs.v1/` へ移動する。8. 別ウィンドウへ切り替えて mzed に戻る。⌘K でパレットを開いて閉じる。9. サイドバーのプロジェクトメニューで別プロジェクトに切り替える |
| **期待結果** | 1. 本文だけ更新され、サイドバーは**一瞬も消えず**ちらつかない。2〜5, 7. サイドバーが更新される（3 の新規サブフォルダ配下も、フォーカス復帰を待たずに出る。7 は `docs.v1/` が消える）。6. サイドバーは変化しない。8. サイドバーが**一瞬も空にならない**（内容が同じなら見た目が変わらない）。9. 従来どおり空になってから新プロジェクトのツリーが出る |
| **備考** | 保存は `Modify(Data)` なので `tree_affected` が false を返す（→ [06](06-rendering-pipeline.md#サイドバー更新の発火条件)）。5 でフォルダを作った直後（数秒以内）に消すと、FSEvents がフラグをまとめて削除が届かないことがある。間隔を空けて確認する |
| **自動化済み** | `src/watcher.rs` の `mdの内容変更はツリー更新しない` / `mdの削除とリネームはツリー更新対象` / `フォルダの作成はツリー更新対象` / `拡張子付きの無視ディレクトリの作成はツリー更新しない` / `拡張子付きディレクトリの無視領域への移動を取り逃がさない` / `消えた非mdファイルは余分に再スキャンしてよい` |

### FILE-11 大きい文書の保存でコードブロックと図を作り直さない

| 項目 | 内容 |
|---|---|
| **前提** | `just bundle` した .app（dev ビルドは highlight.js / mermaid を読めない）。コードブロック数百・Mermaid 数十を含む大きい .md（例: 性能計測用の `big.md`）を表示し、初回の描画が終わっている |
| **手順** | 1. 先頭の見出しを外部エディタで書き換えて保存する。2. コードブロックと Mermaid 図までスクロールする。3. ライト / ダークを切り替える。4. Mermaid 図のソースを 1 つだけ書き換えて保存する |
| **期待結果** | 1. 1 秒以内に見出しが更新される（`MZED_PERF=1` なら `webview.post_render` が初回より桁違いに短い）。2. コードは色付きのまま、図も表示されている。⌘+ホイールのズームとドラッグのパンが効き、クリックでポップアウトする。3. 図が切替先のテーマで描き直される。4. 書き換えた図だけが新しい内容になり、他の図とコードはそのまま |
| **備考** | 同じソースの仕上がりを WebView 側で覚えている（コード 500 件、図 50 件、各 4M 文字まで、LRU）。失敗した図・非表示や寸法 0 で描いた図は覚えない（jsdom ハーネス `cache_rules_after.mjs` で確認）。[06](06-rendering-pipeline.md)「再描画時の再処理キャッシュ」 |

### FILE-12 Cmd+F は大量一致でも止まらず、移動で色が付き直す

| 項目 | 内容 |
|---|---|
| **前提** | 1 文字で数千件以上当たる大きい .md を表示している |
| **手順** | 1. Cmd+F で 1 文字（例: `a`）を速く打ち、続けて数文字足す。2. Enter を何度か押す。3. 先頭で Shift+Enter を押して末尾へ折り返す。4. 検索語を消して `lore` を打ち、1 秒待ってから `m` を打った直後（間を空けず）に Enter を押す |
| **期待結果** | 1. 入力が詰まらず、件数は全件数のまま出る。2. 現在の一致（濃い色）が進み、周りの一致にも薄い色が付いている。3. 文書末尾の一致へ飛び、その周りにも薄い色が付いている。4. `lorem` の 2 件目が現在の一致になる（`lore` の一致を進めた後に 1 件目へ戻されない） |
| **備考** | Range を作るのは現在の一致 ± 500 件だけ。自動化済み: `src/js.rs` の `find_paints_only_a_window_around_the_current_match`（窓の定数）。Range 数の上限は jsdom ハーネスで確認 |

---

## Web serve（SRV）

### SRV-01 `mzed serve` でフォルダがブラウザ表示される

| 項目 | 内容 |
|---|---|
| **前提** | md（mermaid・コードブロック・`$$` 数式・相対 `.md` リンク・サブフォルダ入り）を含むフォルダがある |
| **手順** | `mzed serve <dir>` を実行する |
| **期待結果** | ブラウザが自動で開き、サイドバーツリー＋本文が表示される。mermaid は SVG、コードはハイライト、数式は KaTeX で描画。内部 `.md` リンククリックでページ内遷移。ToC パネルの見出しクリックでスクロール。Ctrl+C でサーバが止まる |

### SRV-02 live-reload とファイル名フィルタ

| 項目 | 内容 |
|---|---|
| **前提** | SRV-01 の状態で表示中 |
| **手順** | (1) 表示中の md を編集して保存する。(2) サイドバー上部のフィルタにファイル名の一部を入力する |
| **期待結果** | (1) 1秒以内にブラウザ表示が更新される（スクロール位置は保持）。(2) ツリーが一致するファイル（と、その祖先フォルダ）だけに絞られる |

### SRV-03 localhost 外・root 外へのアクセス拒否

| 項目 | 内容 |
|---|---|
| **前提** | `mzed serve <dir>` 実行中 |
| **手順** | (1) LAN の別端末（または `ifconfig` の LAN IP 宛て）からアクセスする。(2) `/api/doc?path=../../etc/hosts` や root 外の絶対パスを要求する。(3) `/assets/../Cargo.toml` を要求する |
| **期待結果** | (1) 接続できない（127.0.0.1 bind のみ）。(2) 400。(3) 404 |
| **自動化済み** | `src/serve.rs` の `doc_paramはroot外と非mdを拒否する`, `routeはassetsの外を404にする` |

### SRV-04 テーマ切替

| 項目 | 内容 |
|---|---|
| **前提** | mermaid 図を含む md を serve で表示中 |
| **手順** | ヘッダの ◐ ボタンを押す |
| **期待結果** | ダーク⇄ライトが切り替わり、mermaid 図も新テーマで再描画される。初期表示は OS の外観設定に追従 |

### SRV-05 コマンドパレットから Web 共有を開始・停止する

| 項目 | 内容 |
|---|---|
| **前提** | mzed でプロジェクトを開いている |
| **手順** | 1. Cmd+Shift+P → 「Web Share: Start (Serve in Browser)」を実行する。2. パレットを開き直しラベルを確認する。3. 「Web Share: Stop (…)」を実行する |
| **期待結果** | 1. ブラウザが開き現プロジェクトが表示される。「Web 共有を開始: http://127.0.0.1:6280/（log: …）」の toast が出る。2. ラベルが「Web Share: Stop (http://127.0.0.1:6280/)」に変わっている。3. 「Web 共有を停止しました」の toast が出てサーバに接続できなくなり、ラベルは Start に戻る |
| **備考** | ポートは config の `serve_port`（既定 6280）。同時に動く共有は1つ。ラベルが Start/Stop で状態を示す |
| **自動化済み** | `src/serve.rs` の `toggle_app_shareは起動と停止を往復する`、`src/palette.rs` の `web_shareラベルは稼働状態でstartとstopを切り替える` |

### SRV-06 リクエストログがファイルに残る

| 項目 | 内容 |
|---|---|
| **前提** | Web 共有をアプリ内（または CLI）で起動している |
| **手順** | 1. ブラウザで数ページ閲覧する。2. `tail ~/Library/Logs/mzed/serve.log` を確認する |
| **期待結果** | `<UTC時刻> mzed serve: <ms> <status> <url>` 形式の行が全リクエストぶん記録されている。GUI 起動の mzed.app でも同じく残る（stderr は破棄されるため、ファイルが唯一の恒久出力先） |
| **備考** | 5MB 超で `serve.log.old` に1世代ローテート |
| **自動化済み** | `src/serve.rs` の `実リクエストがログファイルに記録される` |

---

## Mermaid ビューア（MER）

### MER-01 拡大ウィンドウのパン & ズーム

| 項目 | 内容 |
|---|---|
| **前提** | mermaid 図の「別ウィンドウで開く」で拡大ウィンドウを開く |
| **手順** | (1) 修飾キーなしでスクロール / ピンチする。(2) ドラッグする。(3) ダブルクリックする。(4) ツールバーの − / + / 1:1 / Fit を押す |
| **期待結果** | (1) カーソル位置を中心にズームし、ツールバー中央の % 表示が追従する。(2) 図が移動する。(3)(4) Fit は全体表示、1:1 は等倍。% 表示は常に現在値 |
| **自動化済み** | `src/js/mermaid.rs` の `ウィンドウJSは修飾キーなしホイールでズームし率を表示する` ほか |

### MER-02 インライン図のズーム & パン

| 項目 | 内容 |
|---|---|
| **前提** | `tests/fixtures/showcase.md`（mermaid 図を含む）を表示中 |
| **手順** | (1) 図の上で修飾キーなしにスクロールする。(2) ⌘を押しながらスクロール / トラックパッドでピンチする。(3) 図をドラッグする。(4) ドラッグを離した直後の状態を見る。(5) 図をダブルクリックする。(6) 図をクリックする |
| **期待結果** | (1) ページが通常どおりスクロールし、図は拡大しない。(2) **カーソル直下の点が動かないまま**拡大縮小する。(3) 図が移動する。(4) 別ウィンドウは開かない。(5) 等倍に戻り、**別ウィンドウは開かない**。(6) 別ウィンドウで開く（クリックから約 250ms 後） |
| **自動化済み** | `src/js/render.rs` の `インライン図にズームパンが結線される` / `ダブルクリックでポップアウトしない`、`src/js/mermaid.rs` の `ズーム座標をstage原点から測る` |

### MER-03 mindmap の中央ノードと配色

| 項目 | 内容 |
|---|---|
| **前提** | `tests/fixtures/showcase.md` の「マインドマップ」を表示中 |
| **手順** | (1) 中央ノード `mzed` の文字位置を見る。(2) light / dark を切り替える。(3) 同じファイル内の flowchart / sequence / pie / gitGraph を見る |
| **期待結果** | (1) 文字が円の中心にある。(2) どちらのテーマでも全セクションの文字が背景に対して読める。(3) mindmap 以外の図の配色が変わっていない |
| **自動化済み** | `src/js/mermaid.rs` の `mindmap配色のコントラストが4_5以上` / `設定JSONにmindmapラベル補正CSSが含まれる` |

### MER-04 ソースの色指定を mzed 配色が上書きする

| 項目 | 内容 |
|---|---|
| **前提** | 色を指定した mindmap を 3 通り用意する: (a) `%%{init: {'themeVariables': {'cScale1':'#ff0000'}}}%%` を先頭に付ける。(b) frontmatter に `config.themeVariables.cScale1: "#ff0000"` を書く。(c) 先頭に `%% コメント` を置いた上で (a) を付ける |
| **手順** | 表示する |
| **期待結果** | (a)(b)(c) いずれもソースの色指定は無視され、mzed のプリセット配色で描画される。同じ指定を pie に付けた場合はソース側の指定が反映される |

### MER-05 大きい文書の初回表示で図が数秒以内にそろう

| 項目 | 内容 |
|---|---|
| **前提** | `just bundle` した .app。コードブロック数百・Mermaid 数十を含む数 MB の .md（例: 性能計測用の `big.md`）と、`tests/fixtures/showcase.md` を用意する |
| **手順** | 1. `MZED_PERF=1` で起動して大きい .md を開く。2. showcase.md を開き、全図種（flowchart / sequence / class / state / ER / gantt / pie / journey / gitGraph / mindmap / timeline）を見る。3. ライト / ダークを切り替える。4. 図をクリックしてポップアウトする。5. HTML エクスポートする。6. Cmd+F で図の近くの語を検索し、ToC から図の後ろの見出しへ飛ぶ |
| **期待結果** | 1. 先頭の図が 1 秒以内に出始め、2 秒以内にすべてそろう（`webview.post_render` が 1〜2 秒台。修正前は 70 秒超）。2. 図の大きさ・文字位置が以前と同じ。gantt が横幅いっぱいに広がりすぎたり縮んだりしない。mindmap は専用配色、pie / gitGraph は既定配色。3. 図が切替先のテーマで描き直される。4. ポップアウト窓でも図が出て、Fit / ズームが効く。5. エクスポートした HTML に図が入っている（ライト配色）。6. 一致位置へのスクロールと ToC ジャンプが図の有無で狂わない |
| **備考** | 図は隠した iframe の中の mermaid で描き、完成した SVG ノードだけを本文へ移す（[06](06-rendering-pipeline.md)「描画は別文書で行う」）。iframe の DOM は開発者ツールでは `body` 直下の `aria-hidden` な `<iframe>` として見える。自動化済み: `src/js/mermaid.rs` の `ヘルパーJSは別文書で描いて結果のノードだけ移す` / `ヘルパーJSは描画済みの図を飛ばす` / `ヘルパーJSは最初の3枚を先に反映する` |

### MER-06 描画環境が変わっても図の寸法が本文で描いたときと同じ

| 項目 | 内容 |
|---|---|
| **前提** | `just bundle` した .app。`tests/fixtures/showcase.md` を表示中 |
| **手順** | 次の 4 条件でそれぞれ showcase.md を開き直し、flowchart / sequence / gantt / mindmap の図を見る。(1) `code_font` を別のフォントにする。(2) `code_font_size` を大きくする（例: 20）。(3) `zoom` を 1.5 にする（Cmd++ でも可）。(4) Cmd+\\ で分割し、狭くしたペイン側に表示する |
| **期待結果** | どの条件でも、ノードの文字が枠からはみ出したり切れたりせず、図がカード幅を超えない。gantt はペイン幅に合わせて描かれる。(4) の後にペインを広げても図は崩れない（描いた時の幅のまま。再描画で追従） |
| **備考** | iframe 内の stage に元の `pre` の幅と文字関係のスタイルを写して計測を合わせている（[06](06-rendering-pipeline.md)「描画は別文書で行う」） |

### MER-07 iframe で描けないとき・描けない図があるとき

| 項目 | 内容 |
|---|---|
| **前提** | `just bundle` した .app。構文エラーの図を 1 つ挟んだ .md（前後は正常な図）と、`%%{init: {"arrowMarkerAbsolute": true, "flowchart": {"arrowMarkerAbsolute": true}}}%%` を付けた flowchart を用意する |
| **手順** | 1. 構文エラー入りの .md を開く。2. 正常な図のソースを書き換えて保存する。3. arrowMarkerAbsolute の flowchart を開く |
| **期待結果** | 1. 構文エラーの図はソースのまま表示され、前後の図は SVG で出る。2. 保存後も同じ（エラーの図は再試行されソースのまま、他は更新される）。3. 矢印の先端が表示される。カードの下の余白が約 9px 広いのは既知の差（06 参照） |
| **備考** | iframe の読み込み失敗・`mermaid` 未定義時に本文描画へ切り替わることは、スクリプト URL を差し替えた検証ビルドで確認済み（`z-ai/2026-09-24-perf/results.md` Step 4 追記 1）。自動化済み: `src/js/mermaid.rs` の `ヘルパーJSは失敗したiframeを捨てて本文で描く` / `ヘルパーJSは成功した図だけを本文へ移す` / `ヘルパーJSはiframeで描いた矢印マーカーをローカル参照に直す` |

---

## バンドル配布（DIST）

### DIST-01 bundle → dmg 生成

| 項目 | 内容 |
|---|---|
| **手順** | `just bundle` を実行する |
| **期待結果** | `target/dx/mzed/bundle/macos/macos/` に `.app` と `.dmg` が生成される |

### DIST-02 .app 単体起動（dev 環境なし）

| 項目 | 内容 |
|---|---|
| **前提** | `just install` または dmg から `/Applications/mzed.app` をインストール済み |
| **手順** | dev 環境（`dx serve` 等）を起動せずに `/Applications/mzed.app` を起動する |
| **期待結果** | assets/JS が同梱されており正常に動作する |

### DIST-03 CLI symlink（PATH 設定後）

| 項目 | 内容 |
|---|---|
| **前提** | `just install` 済み。`~/.local/bin` を PATH に追加済み |
| **手順** | 新しいターミナルで `mzed --version` を実行する |
| **期待結果** | バージョンが表示される |

### DIST-04 uninstall

| 項目 | 内容 |
|---|---|
| **前提** | `just install` 済み |
| **手順** | `just uninstall` を実行する |
| **期待結果** | `/Applications/mzed.app` と `~/.local/bin/mzed` が削除される |

---

## B1〜B4 修正テストケース

### B1-01 ファジー検索（Cmd+P）からの開いたファイルがサイドバーで展開される

| 項目 | 内容 |
|---|---|
| **前提** | `docs/deep/nested.md` のような深いファイルを含むプロジェクトが開いている。サイドバーはすべて折りたたまれている |
| **手順** | Cmd+P → ファイル名を入力 → `nested.md` を選択・Enter |
| **期待結果** | `docs/` と `docs/deep/` がサイドバーで展開され、`nested.md` がハイライトされる |

### B1-02 全文検索（Cmd+Shift+F）からの開いたファイルがサイドバーで展開される

| 項目 | 内容 |
|---|---|
| **前提** | 深い階層にある .md ファイルにユニークなキーワードが含まれている。サイドバーは折りたたまれている |
| **手順** | Cmd+Shift+F → キーワードを入力 → 結果をクリックまたは Enter |
| **期待結果** | 開いたファイルの親フォルダがすべて展開され、サイドバーで位置が分かる |

---

### B2-01 パスコピー成功時にエラートーストが出ない

| 項目 | 内容 |
|---|---|
| **前提** | サイドバーにファイルが表示されている |
| **手順** | ファイルを右クリック → 「パスをコピー」をクリック |
| **期待結果** | 「Copied!」トーストが表示される。エラートーストは表示されない。クリップボードに絶対パスが入っている |

### B2-02 フォルダの絶対パスコピー

| 項目 | 内容 |
|---|---|
| **前提** | サイドバーにフォルダが表示されている |
| **手順** | フォルダを右クリック → 「パスをコピー」をクリック |
| **期待結果** | 「Copied!」トーストが表示される。クリップボードにフォルダの絶対パスが入っている |

### B2-03 相対パスコピーもエラーなしで動作する

| 項目 | 内容 |
|---|---|
| **前提** | サイドバーにファイルが表示されている |
| **手順** | ファイルを右クリック → 「相対パスをコピー」をクリック |
| **期待結果** | 「Copied!」トーストのみ表示。クリップボードにプロジェクトルート基準の相対パスが入っている |

---

### B3-01 プロジェクトオープン後に作成したファイルが全文検索にヒットする

| 項目 | 内容 |
|---|---|
| **前提** | プロジェクトが mzed で開いている |
| **手順** | 1. プロジェクトフォルダに新しい .md ファイルを作成する  2. Cmd+Shift+F で全文検索を開く  3. 新しいファイルにしか含まれないキーワードを入力する |
| **期待結果** | 新しいファイルが検索結果に現れる |
| **自動化済み** | `src/search.rs` の `プロジェクトオープン後に作成したファイルが検索にヒットする` |

### B3-02 ファジー検索（Cmd+P）でも新規ファイルが表示される

| 項目 | 内容 |
|---|---|
| **前提** | プロジェクトが開いている |
| **手順** | 1. プロジェクトフォルダに新しい .md ファイルを作成する  2. Cmd+P でファジー検索を開く  3. 新しいファイル名を入力する |
| **期待結果** | 新しいファイルが候補に表示される |

---

### B4-01 設定 OFF 時は最新ファイルを自動で開かない（デフォルト）

| 項目 | 内容 |
|---|---|
| **前提** | 設定 → 一般 → 「プロジェクトを開いたとき最新ファイルを自動で開く」が OFF |
| **手順** | タブ履歴がないプロジェクトに切替える |
| **期待結果** | 既存の `pick_markdown` 挙動（README 優先）でファイルが開く |

### B4-02 設定 ON 時は最終更新ファイルが自動で開く

| 項目 | 内容 |
|---|---|
| **前提** | 設定 → 一般 → 「プロジェクトを開いたとき最新ファイルを自動で開く」を ON にする |
| **手順** | タブ履歴がない（初回訪問）プロジェクトに切替える |
| **期待結果** | プロジェクト内で最終更新日時が最も新しい .md ファイルが自動的に開く |

### B4-03 既存タブがある場合は復元タブを優先する

| 項目 | 内容 |
|---|---|
| **前提** | 設定 ON。プロジェクトAで `a.md` を開いて別プロジェクトに切替える |
| **手順** | プロジェクトAに戻る |
| **期待結果** | `a.md` が復元される（最新ファイルでなくても `a.md` が開く）|
| **自動化済み** | `src/services/file_service.rs` の `latest_markdown*` テスト群 |

---

## 行間設定（B5）

### B5-01 行間コントロールが即時反映される

| 項目 | 内容 |
|---|---|
| **前提** | Markdown ファイルを表示中。設定 Appearance を開く |
| **手順** | 「行間」の `+` ボタンを押して値を上げる |
| **期待結果** | ビューア本文の行間がリアルタイムに広がる（ページリロード不要） |

### B5-02 行間設定が再起動後も保持される

| 項目 | 内容 |
|---|---|
| **前提** | 設定 Appearance で行間を 2.0 に変更してアプリを終了する |
| **手順** | アプリを再起動して設定 Appearance を開く |
| **期待結果** | 行間が 2.0 のまま表示される。本文の行間も 2.0 相当になっている |

### B5-03 行間は 1.2〜2.4 の範囲外に出ない

| 項目 | 内容 |
|---|---|
| **前提** | 設定 Appearance 「行間」コントロール |
| **手順** | `−` ボタンを連打して最小値を超えようとする、続けて `+` ボタンで最大値を超えようとする |
| **期待結果** | 1.2 以下・2.4 以上にならない（UI ボタン操作・CSS 適用ともに） |

### B5-04 行間リセットで 1.7 に戻る

| 項目 | 内容 |
|---|---|
| **前提** | 行間を 2.0 に設定済み |
| **手順** | 「リセット」ボタンをクリックする |
| **期待結果** | 行間が 1.7 に戻り、本文の行間も既定値になる |

---

## KaTeX 数式（R-06）

### R-06-01 通貨記号が数式化されない

| 項目 | 内容 |
|---|---|
| **前提** | KaTeX 有効。`$47K` や `$100` を含む Markdown を表示中 |
| **手順** | レンダリング結果を目視確認する |
| **期待結果** | `$47K` が数式レンダリングされず、テキストそのまま表示される |
| **自動化済み** | `src/js/render.rs` の `katexデリミタに単一ドルが含まれない` |

### R-06-02 `\(...\)` インライン数式が正しく描画される

| 項目 | 内容 |
|---|---|
| **前提** | KaTeX 有効。`\(x^2 + y^2 = z^2\)` を含む Markdown を表示中 |
| **手順** | レンダリング結果を目視確認する |
| **期待結果** | 数式がインラインで KaTeX レンダリングされる |

### R-06-03 `$$...$$` ブロック数式が正しく描画される

| 項目 | 内容 |
|---|---|
| **前提** | KaTeX 有効。`$$E = mc^2$$` を含む Markdown を表示中 |
| **手順** | レンダリング結果を目視確認する |
| **期待結果** | 数式がブロック（センタリング）で KaTeX レンダリングされる |

---

## Task View（TV）

### TV-01 Cmd+Shift+D でトグル

| 項目 | 内容 |
|---|---|
| **前提** | mzed が起動中。`feature_task_view` が ON（既定） |
| **手順** | Cmd+Shift+D を押す |
| **期待結果** | Task View モードが開く（左ペインにタスクツリー、右ペインに選択誘導メッセージ）。再度 Cmd+Shift+D または Esc を押すと閉じて通常画面に戻る |

### TV-02 This Project ⇔ All Projects 切替

| 項目 | 内容 |
|---|---|
| **前提** | Task View を開いている |
| **手順** | 左ペイン上部の「This Project」→「All Projects」ボタンをクリックする。さらに「This Project」に戻す |
| **期待結果** | This Project は現在のプロジェクトのタスクのみ表示（全期間）。All Projects は直近 7 日（既定）に作成したタスクを表示し、日数セレクタが現れる |

### TV-03 All Projects 日数フィルタ

| 項目 | 内容 |
|---|---|
| **前提** | Task View、All Projects モード。`task_view_scan_roots` が設定済みで複数プロジェクトにタスクがある |
| **手順** | 日数セレクタを「7 日」→「30 日」に変更する |
| **期待結果** | 30 日以内に作成（`created` フィールド）したタスクが追加表示される。7 日超 30 日以内のタスクが現れる（存在する場合） |

### TV-04 タスクフォルダの status 色

| 項目 | 内容 |
|---|---|
| **前提** | Task View を開いている。異なる status（todo / in_progress / review / done）のタスクフォルダが存在する |
| **手順** | 左ペインのタスクフォルダ行を目視確認する |
| **期待結果** | todo=#8b949e / in_progress=#1f6feb / review=#d29922 / done=#3fb950 の色で status ドットが表示される |

### TV-05 タスク詳細表示

| 項目 | 内容 |
|---|---|
| **前提** | Task View を開き、タスクフォルダが存在する |
| **手順** | タスクフォルダ行をクリックする |
| **期待結果** | 右ペインに `task.md` の Markdown がレンダリングされる。左ペインの行がアクティブハイライト（左ボーダー青）される。フォルダが展開され task.md サブ行が表示される |

### TV-06 feature OFF 時の無効化

| 項目 | 内容 |
|---|---|
| **前提** | 設定 → Features →「Task View を有効にする」を OFF にする |
| **手順** | Cmd+Shift+D を押す |
| **期待結果** | Task View が開かない（何も変化なし） |

### TV-07 scan_roots 空時の All Projects ヒント

| 項目 | 内容 |
|---|---|
| **前提** | `task_view_scan_roots` が空（設定未済）。Task View → All Projects を開く |
| **手順** | All Projects モードに切り替える |
| **期待結果** | 現プロジェクトのタスクのみ表示（scan_roots が空なので fallback）。設定への誘導テキストが左ペインに表示される（タスクが 0 件の場合） |

### TV-08 広い親ディレクトリを scan_root に指定して深いリポを発見する

| 項目 | 内容 |
|---|---|
| **前提** | `task_view_scan_roots` に `~/dev` や `~` のような広い親ディレクトリを指定済み。複数の git リポ（例: `~/dev/repos/org/proj-a`, `~/dev/repos/org/proj-b`）が `docs/memo/tasks/` 以下にタスクフォルダを持つ |
| **手順** | Task View → All Projects に切り替える |
| **期待結果** | スキャンルート直下でなく任意の深さにあるリポのタスクが発見・一覧表示される。`node_modules` / `target` / `Library` / `.git` / 先頭 `.` のディレクトリ内は走査されない（ログやタイムアウトなどで確認）。深さ上限（10 段）を超える場所にあるタスクは表示されない |
| **備考** | 枝刈り付き再帰 walk（`discover_projects`）による発見。`~` 全体を指定した場合でも UI は止まらない（`spawn_blocking` で実行）。2 回目以降は OS のディレクトリキャッシュで高速化される |

### TV-09 リポ境界（.git）でスキャンが止まる

| 項目 | 内容 |
|---|---|
| **前提** | `task_view_scan_roots` に `~/dev/repos` のような親を指定。`~/dev/repos/org/my-repo/.git` が存在し、`~/dev/repos/org/my-repo/docs/memo/tasks/` にタスクがある |
| **手順** | Task View → All Projects に切り替え、スキャンが完了するのを待つ |
| **期待結果** | `my-repo` のタスクが一覧に表示される。`my-repo` の内部（`src/`・`target/` 等）は再帰されない（`my-repo` 以下の「子リポ」扱いのディレクトリがあっても別プロジェクトとして表示されない）。スキャンが高速に終わる |
| **備考** | `.git` ディレクトリの存在で `discover_projects` がそのリポ内部への再帰を打ち切る。単体テスト `discover_projects_stops_at_repo_boundary` / `discover_projects_repo_without_tasks_is_not_adopted` で境界条件を担保済み |
| **自動化済み** | `src/services/task_scan.rs` の `discover_projects_stops_at_repo_boundary`, `discover_projects_repo_without_tasks_is_not_adopted` |

### TV-10 セッションキャッシュで再スキャンが省略される

| 項目 | 内容 |
|---|---|
| **前提** | Task View が開いており、All Projects スキャンが完了している |
| **手順** | Task View を Esc で閉じて Cmd+Shift+D で再度開く（スコープ・日数は変えない） |
| **期待結果** | 「スキャン中…」が表示されず、即座にタスク一覧が表示される（セッションキャッシュからの表示） |
| **備考** | キャッシュキーは (scope, scan_roots, subpath, days) の組み合わせ。セッション内のみ保持（再起動で消える） |

### TV-11 ↻ ボタンで強制再スキャンできる

| 項目 | 内容 |
|---|---|
| **前提** | Task View が開いており、タスク一覧が表示済み（キャッシュ済み状態） |
| **手順** | 左ペインヘッダの「↻」ボタンをクリックする |
| **期待結果** | 「スキャン中…」が一瞬表示され、最新のファイルシステム状態でタスク一覧が再表示される。直前に追加したタスクが反映される |
| **備考** | ↻ クリックで現在キーのキャッシュエントリが削除され、`discover_projects` が再実行される |

### TV-12 行ホバーで絶対パスコピーボタンが出る

| 項目 | 内容 |
|---|---|
| **前提** | Task View が開いており、タスクフォルダまたはファイル行が表示されている |
| **手順** | タスクフォルダ行・`task.md` 行・成果物ファイル行それぞれにマウスを乗せる |
| **期待結果** | 行右端にクリップボードアイコンのコピーボタン（`.mdo-copy-path`）が表示される。クリックすると絶対パスがクリップボードに入り「Copied!」トーストが出る |
| **備考** | サイドバーの `.mdo-copy-path` と同じ CSS・スタイルトークンを踏襲。相対パスではなく `canonical_clipboard_text` による絶対パス |

### TV-13 プロジェクトルート行の絶対パスコピー

| 項目 | 内容 |
|---|---|
| **前提** | Task View が開いており、All Projects モードで複数プロジェクトが表示されている |
| **手順** | プロジェクトルート行（太字のプロジェクト名行）にホバーし、コピーボタンをクリックする |
| **期待結果** | そのプロジェクトのルートディレクトリの絶対パスがクリップボードに入り「Copied!」トーストが出る。別プロジェクトのルートでも同様に正しい絶対パスが入る（root 非依存） |

### TV-14 行を右クリックで専用メニューが出る

| 項目 | 内容 |
|---|---|
| **前提** | Task View が開いており、タスクフォルダ・ファイル行が表示されている |
| **手順** | (1) タスクフォルダ行を右クリックする。(2) `task.md` 行を右クリックする。(3) 成果物ファイル行を右クリックする。(4) プロジェクトルート行を右クリックする |
| **期待結果** | (1)(2)(3) いずれも独自メニューが `position: fixed` で出る。背景クリックで閉じる。メイン画面のサイドバーメニュー（リネーム・削除を含む）は出ない |
| **備考** | Task View 専用の `TaskCtxMenu` overlay。リネーム・削除は含まない |

### TV-15 右クリックメニュー: ファイル行の全項目

| 項目 | 内容 |
|---|---|
| **前提** | Task View を開き、`task.md` 行または成果物ファイル行を右クリックする |
| **手順** | メニューの各項目を順にクリックする |
| **期待結果** | 「絶対パスをコピー」→ 絶対パスがクリップボードに入り「Copied!」トースト。「Finder で表示」→ そのファイルを Finder で選択表示。「デフォルトアプリで開く」→ OS のデフォルトアプリで開く。「お気に入りに追加」→ サイドバーのお気に入りに追加され、次回再クリックで「お気に入りから外す」に変わる |
| **備考** | ファイル行（`is_dir: false`）のみ「デフォルトアプリで開く」が表示される |

### TV-16 右クリックメニュー: ディレクトリ行の項目出し分け

| 項目 | 内容 |
|---|---|
| **前提** | Task View を開き、タスクフォルダ行またはプロジェクトルート行を右クリックする |
| **手順** | メニューの項目を確認する |
| **期待結果** | 「デフォルトアプリで開く」が表示されない。タスクフォルダ行では「お気に入りに追加/外す」が表示される。プロジェクトルート行では「お気に入り」項目も表示されない（絶対パスをコピー・Finder のみ） |
| **備考** | ディレクトリを「デフォルトで開く」は Finder と同義のため省略。プロジェクトルートはタスクの管理単位ではないためお気に入りも対象外 |

### TV-17 This Project のライブ再スキャン

| 項目 | 内容 |
|---|---|
| **前提** | Task View（This Project）を開いたままにする |
| **手順** | 別ターミナルで `docs/memo/tasks/` に新しいタスクフォルダ＋task.md を作成、または既存 task.md の `status` を書き換える |
| **期待結果** | ↻ を押さなくても数秒以内に一覧へ自動反映される（新タスク出現・status ドット色変化）。All Projects スコープではローカル保存で他リポの再走査が走らない |

### TV-18 Cmd+R で再スキャン

| 項目 | 内容 |
|---|---|
| **前提** | Task View を開く（All Projects 推奨） |
| **手順** | 他リポにタスクを追加してから Cmd+R を押す |
| **期待結果** | 一覧が再走査され新タスクが出る。Task View を閉じた状態で Cmd+R を押しても何も起きない |

### TV-19 左ペインのリサイズ

| 項目 | 内容 |
|---|---|
| **前提** | Task View を開く |
| **手順** | 左ペインと右ペインの境界をドラッグする |
| **期待結果** | 左ペイン幅が 200〜600px の範囲で追従する。ドラッグ解除後も維持される（アプリ再起動での永続化は対象外） |

### TV-20 足場フォルダ配下のネストしたプロジェクトを発見する

| 項目 | 内容 |
|---|---|
| **前提** | `task_view_scan_roots` の配下に、`docs/memo/tasks` を持つ非 repo フォルダ（例 `hoge`）があり、さらにその子（例 `hoge/workspace`）も `docs/memo/tasks` を持つ |
| **手順** | Task View → All Projects に切り替える |
| **期待結果** | `hoge` と `hoge/workspace` の両方がプロジェクトとして表示される。tasks ツリー自体（`hoge/docs` 配下）は別プロジェクトとして走査されない |
| **自動化済み** | `src/services/task_scan.rs` の `discover_projects_finds_nested_project_under_adopted_dir`, `discover_projects_nested_stops_at_repo_boundary` |

### TV-21 プロジェクトルートを scan_root に直接指定できる

| 項目 | 内容 |
|---|---|
| **前提** | `task_view_scan_roots` にプロジェクトルート自体（`<root>/docs/memo/tasks` を持つパス）を指定 |
| **手順** | Task View → All Projects に切り替える |
| **期待結果** | scan_root 自身がプロジェクトとして表示される（repo 内部のディレクトリを指定した場合も同様に歩ける） |
| **自動化済み** | `src/services/task_scan.rs` の `scan_all_projects_adopts_scan_root_itself` |

### TV-22 タスクフォルダ内の全ファイル・サブフォルダが表示される

| 項目 | 内容 |
|---|---|
| **前提** | タスクフォルダに `task.md` のほか、md・画像等のファイルと、ファイルを含むサブディレクトリ（例 `mvp/assets/logo.png`）がある |
| **手順** | Task View でタスクフォルダを展開する。(1) md ファイル行をクリック。(2) 非 md ファイル行（画像等）をクリック。(3) サブフォルダ行をクリックして展開し、さらに深い階層も辿る |
| **期待結果** | フォルダ内の全エントリが表示される（フォルダ優先・名前順、`.` 始まりと空フォルダは出ない）。(1) md は右ペインに描画される。(2) 非 md は OS の既定アプリで開く。(3) サブフォルダが chevron で開閉でき、配下のファイルも (1)(2) と同じ挙動 |
| **自動化済み** | `src/services/task_scan.rs` の `list_task_files_lists_all_but_task_md_and_dotfiles`, `list_task_files_recurses_into_subdirectories` |

### TV-23 Ctrl+Tab でスコープ切替

| 項目 | 内容 |
|---|---|
| **前提** | Task View を開く（This Project 表示） |
| **手順** | (1) Ctrl+Tab を押す。(2) もう一度 Ctrl+Tab を押す。(3) Task View を閉じ、タブを2つ以上開いて Ctrl+Tab を押す |
| **期待結果** | (1) All Projects に切り替わる。(2) This Project に戻る（選択中タスクは解除）。(3) 従来どおり次のタブに切り替わる |
| **備考** | `task_view_toggle_scope`（リバインド可、既定 Ctrl+Tab）。Task View が閉じているときは固定の次タブ切替へフォールバック |
### TV-24 ステータスでグループ化する

| 項目 | 内容 |
|---|---|
| **前提** | 複数のステータス（todo / in_progress / review / done）のタスクがあるプロジェクトを Task View で開く |
| **手順** | (1) 既定状態のツリーを見る。(2) 設定 → 機能 → Task View の「ステータスの並び順」で「完了」を先頭に上げる。(3) 「階層の順序」を「ステータス → プロジェクト」に変える。(4) 「ステータスでグループ化」を OFF にする |
| **期待結果** | (1) プロジェクト見出しの下に「未着手 / 対応中 / レビュー / 完了」の見出しが色ドット＋件数付きで並び、各見出しは chevron で折り畳める。frontmatter が壊れた/欠落したタスクは「その他」として末尾に出る。(2) 見出しの並びが即座に入れ替わる。(3) ステータスが最上位になり、その下に各プロジェクトが並ぶ。(4) 従来どおりプロジェクト直下にタスクがフラットに並ぶ |
| **備考** | 設定は `task_view_group_by_status` / `task_view_group_order` / `task_view_status_order`。設定画面には現在の設定に応じたツリー形状の ASCII プレビューが出る（サンプル名は `project-a` / `project-b` の汎用名） |
| **自動化済み** | `src/services/task_scan.rs` の `build_groups_*` 各テスト |

### TV-25 タスクの日付並び順を切り替える

| 項目 | 内容 |
|---|---|
| **前提** | 同一グループに `created` の異なるタスクが複数ある |
| **手順** | 設定 → 機能 → Task View の「日付の並び順」を「古い順（昇順）」に変える |
| **期待結果** | 各グループ内のタスクが `created` 昇順に並び替わる（既定は降順＝新しい順）。グループ化 OFF のときも同様に効く |
| **自動化済み** | `src/services/task_scan.rs` の `build_groups_flat_ascending_reverses_task_order` |

### TV-26 右ペインで mermaid / ハイライト / KaTeX が描画される

| 項目 | 内容 |
|---|---|
| **前提** | `task.md` に mermaid コードブロック・言語付きコードブロック・（KaTeX ON なら）数式を含むタスクがある |
| **手順** | Task View でそのタスクを選択して右ペインに表示する |
| **期待結果** | mermaid が SVG 図として描画され、コードブロックがシンタックスハイライトされ、数式が KaTeX で描画される（メインビューと同等） |
| **備考** | Task View のドキュメントロードごとに `doc_tick` が bump され、app の post-render effect が再実行される |

---

## オーバーレイ一覧の選択（OVL）

### OVL-01 プロジェクトメニューでホバーとキーボードが同じ選択を共有する

| 項目 | 内容 |
|---|---|
| **前提** | Cmd+O の候補が画面に収まらない数（10件以上）ある |
| **手順** | 各ステップは**最後に矢印キーを押してから 1 秒以上待ってから**行う。1. Cmd+O を開き、いずれかの候補行にマウスを乗せる（開いた直後、キーを押さずに）。2. 別の候補行へマウスを動かす。3. マウスを静止させたまま ↓ を数回押す。4. さらに ↓ を押してリストの下端より先へ選択を進める。5. マウスを別の行へ動かしてから Enter を押す |
| **期待結果** | 1. 開いた直後でもカーソルの下の行がハイライトされる（キー操作を待たない）。2. 移動先の行だけが青くハイライトされ、元の行のハイライトは消える。どちらもリストはスクロールしない。3. ハイライトが1行ずつ下へ動き、カーソルの下の行に戻らない。4. 選択行が見えるようにリストが自動スクロールする。5. 最後にホバーした行のプロジェクトが開く |
| **備考** | ホバーとキーボードは単一の選択インデックスを共有する。末尾の「Open Folder…」行も同じくホバーで選択できる |

### OVL-02 コマンドパレット / ファイル検索でも同じ選択挙動になる

| 項目 | 内容 |
|---|---|
| **前提** | Cmd+Shift+P（コマンド一覧）と Cmd+P（ファイル検索。候補が画面に収まらない数） |
| **手順** | 両方のモードで OVL-01 の 1〜5 と同じ操作をする（同じく矢印キーから 1 秒以上空ける） |
| **期待結果** | OVL-01 と同じ。ホバー行だけがハイライトされ、Enter でその行のコマンドが実行される（ファイル検索ではそのファイルが開く）。矢印キーでの移動がホバーに戻されず、ホバーではリストがスクロールしない |

### OVL-03 矢印キー直後（250ms 以内）のホバーは無視される

| 項目 | 内容 |
|---|---|
| **前提** | Cmd+O または Cmd+P で候補が画面に収まらない数ある |
| **手順** | 1. ↓ を押した直後（0.25 秒以内）に、マウスを別の行へ素早く動かす。2. そのままマウスを止めて 1 秒待ち、さらに 1 行分だけマウスを動かす。3. ↓ を押しっぱなしにして選択を高速に送る（マウスはリスト上に置いたまま静止させる） |
| **期待結果** | 1. ハイライトは矢印キーで進んだ行に留まり、カーソルの下の行へ移らない。2. 動かした先の行にハイライトが移る（ガード経過後はホバーが効く）。3. ハイライトが止まらずに送られ、カーソルの下の行へ引き戻されない |
| **備考** | ガードは最後のキー操作から 250ms（`HOVER_KEY_GUARD`）。矢印キーでリストがスクロールし、静止したカーソルの下に別の行が入って `mouseenter` が発火するのを打ち消すための措置 |
| **自動化済み** | `src/ui.rs` の `hover_takes_selection` 各テスト（初回・ガード内・ガード経過後・同一行）。実際の `mouseenter` 発火は手動確認 |

### OVL-04 Cmd+P でフォルダ名を含むクエリでもファイルが当たる

| 項目 | 内容 |
|---|---|
| **前提** | `docs/specs/05-zed-integration.md` のようなネストしたファイルがあるプロジェクトを開いている。比較する候補は既読状態が同じにする（未読ファイルは tier 順に関わらず先頭に来るため、tier 順の確認は未読が絡まない候補同士で行う） |
| **手順** | 1. Cmd+P で `specs/05` と入力する。2. 別ウィンドウで `plan` のようにファイル名の拡張子なし一部だけを入力し、同名で新しいものと古いものがあれば比較する |
| **期待結果** | 1. ファイル名にその文字列を含まなくても、ルートからの相対パスに `specs/05` を含むファイルがヒットする。2. `plan.md` のように完全一致するファイルが最上位に来て、同点（完全一致同士）は更新が新しい方が上に来る。候補行はファイル名の右にディレクトリが薄い色で並ぶ |
| **自動化済み** | `src/fuzzy.rs` の `rank_tiered` 各テスト（tier 境界・拡張子なし一致・同 tier recency 順・空クエリ・フォルダ名一致）。実際の描画・並びは手動確認 |

### OVL-05 Cmd+O は現在のプロジェクト→完全一致→最後に開いた順

| 項目 | 内容 |
|---|---|
| **前提** | mzed で複数プロジェクトを開いた履歴がある（`project_history` に記録が残っている） |
| **手順** | 1. あるプロジェクトを開いた状態で Cmd+O を開き、何も入力せず一覧の先頭を見る。2. 検索欄にプロジェクト名の一部やエイリアスの一部、あるいはパスの断片を入力する |
| **期待結果** | 1. 現在開いているプロジェクトが常に先頭に固定される。2. 残りは一致度（完全一致→前方一致→部分一致→ファジー）順に並び、同じ一致度の中では mzed でそのプロジェクトを最後に開いた時刻が新しい順に並ぶ。エイリアスの一部やパスの一部の入力でもヒットする |

### OVL-06 Cmd+P の候補は 200 件まで描き、残りは件数だけ出す

| 項目 | 内容 |
|---|---|
| **前提** | Markdown が 201 件以上あるプロジェクトを開いている |
| **手順** | 1. Cmd+P を開き、何も入力しない。2. ↑ を押す（先頭から末尾へ折り返す）。3. Enter を押す。4. もう一度 Cmd+P を開き、候補が 200 件以下になるまで絞り込む |
| **期待結果** | 1〜2. リスト末尾に「他 N 件。絞り込んでください」（N = 全件 − 200）が出て、その行は選択できない。↑ で 200 件目の行が選ばれる。3. その行のファイルが開く。4. 注記が消える |
| **備考** | 完全一致の候補は 201 件目以降に落ちても描画に残る（未読の部分一致 200 件 + 既読の完全一致 1 件なら、完全一致が最後の行に出て Enter で開ける） |
| **自動化済み** | `src/ui/command_palette.rs` の `上限を超えた候補は描画せず件数だけ返す` / `上限以内なら全件描画し残りは0` / `上限の外に落ちる完全一致も描画に残す`。描画と選択の折り返しは手動確認 |

---

## 未読マーカー（UNR）

### UNR-01 エージェントが作った新規 md がサイドバーで未読になる

| 項目 | 内容 |
|---|---|
| **前提** | プロジェクトを開いた状態で、サイドバーに表示中のフォルダ配下に新しい `.md` を作る（エージェントの書き込み、または `echo >> file.md` でも可） |
| **手順** | 1. 対象フォルダ配下に新規 `.md` を作成、または既存ファイルを更新する。2. サイドバーの反映を待つ |
| **期待結果** | 新規/更新したファイルの行にファイル名の前へ直径 6px の緑丸が付く。祖先フォルダの行の右端に、配下の未読件数が薄い文字で表示される（0件のフォルダには何も出ない）。同じフォルダ内では未読ファイルが先頭にまとまり、更新時刻（mtime）の新しい順に並ぶ。既読ファイルはそれに続けて名前順、ディレクトリは常にファイルより前で名前順 |
| **備考** | 内容だけの更新（ファイル名・構成は変わらない保存）は、ツリーの全再スキャンではなく変更ファイルだけの mtime 差し替えで反映される → UNR-07 |

### UNR-02 未読ファイルを開くと緑丸が消える

| 項目 | 内容 |
|---|---|
| **前提** | UNR-01 の状態（緑丸が付いたファイルがある） |
| **手順** | サイドバーまたは Cmd+P からそのファイルを開く |
| **期待結果** | 緑丸が消え、祖先フォルダの未読件数がその分だけ減る |

### UNR-03 プロジェクトを初めて開いたときは全部緑にならない

| 項目 | 内容 |
|---|---|
| **前提** | mzed で一度も開いたことのないプロジェクト（`project_history` に記録が無い）。複数 root（マルチルート Zed ワークスペース）があれば両方用意する |
| **手順** | そのプロジェクトを初めて開く |
| **期待結果** | 既存ファイルは（開く前の更新であれば）未読扱いにならず、緑丸が付かない。開いた後に更新・作成されたファイルだけが未読になる。マルチルートの場合、セカンダリ root 配下のファイルも同じ規則に従う（root ごとに `first_opened_at` が記録される。プライマリだけでなく全 root で確認する） |

### UNR-04 ツールバーの未読バッジから一覧を開いて既読にできる

| 項目 | 内容 |
|---|---|
| **前提** | 未読ファイルが1件以上ある。マルチルートなら複数 root にまたがって未読を用意する |
| **手順** | 1. コンテンツ右上のツールバーに出る「緑丸+件数」バッジにマウスを乗せる（またはクリックして固定表示する）。2. ポップオーバーに出たファイル行をクリックする。3. 再度バッジをホバー/クリックし、末尾の「すべて既読にする」を押す |
| **期待結果** | 1. 未読ファイルが root を問わず最大20件、更新が新しい順に並ぶ。クリックでの固定表示はもう一度クリックすると閉じる。2. クリックしたファイルが開き、一覧・サイドバー・Cmd+P の緑丸が消える（ポップオーバーの固定表示も閉じる）。3. 現在のプロジェクトの全 root の未読が一括で消え、バッジ自体も（未読0件になるので）消える |

### UNR-05 Cmd+P では未読ファイルが先頭に緑丸付きで出る

| 項目 | 内容 |
|---|---|
| **前提** | 未読ファイルが1件以上ある |
| **手順** | Cmd+P を開き、検索語を入力せず／入力して候補を見る |
| **期待結果** | 未読ファイルは一致順位（tier）に関わらず候補の先頭に来て、行頭に緑丸が付く |

### UNR-06 コマンドパレットの Mark All as Read

| 項目 | 内容 |
|---|---|
| **前提** | 未読ファイルが1件以上ある |
| **手順** | Cmd+Shift+P で「Mark All as Read」を実行する |
| **期待結果** | UNR-04 の「すべて既読にする」と同じ結果になる（現在のプロジェクトの未読がすべて消える） |
| **自動化済み** | `src/app_state/unread.rs` の各テスト（記録あり/なし判定、一括既読、表示中更新、pruned）、`src/palette.rs` の `一括既読コマンドがreadクエリで返る`。実際の描画・クリック連動は手動確認 |

### UNR-07 既存 md への追記（リネームなし）だけでも未読になる

| 項目 | 内容 |
|---|---|
| **前提** | プロジェクトを開いた状態で、既に既読の md ファイルがある |
| **手順** | エディタ等でそのファイルを開いたままにせず、`echo "追記" >> file.md` のように中身だけを保存する（ファイル名・親ディレクトリは変えない） |
| **期待結果** | ツリー全体の再スキャン（サイドバーのちらつき）無しに、そのファイルだけ未読（緑丸）になる。表示中のファイルであれば、ライブリロードで再表示された時点で既読のまま留まる |
| **自動化済み** | `src/watcher.rs` の `classify_tree_change` 系テスト（内容変更の分類、構造変化優先）、`src/files.rs` の `update_mtimesは対象ノードとその祖先だけ更新する`。実際の fs イベント経路・画面反映は手動確認 |

---

## メモ（NOTE）

### NOTE-01 選択 → アイコン → Enter で JSON が1件できる

| 項目 | 内容 |
|---|---|
| **前提** | 見出しを含む Markdown をプロジェクトから開いている |
| **手順** | 1. 見出しの下の本文を1段落ドラッグで選択して指を離す。2. 選択終端に出た丸いアイコンをクリックする。3. 入力欄に「ここを直して」と入力して Enter |
| **期待結果** | 1. 選択範囲の終端（右上）に 24px の丸い吹き出しアイコンが浮く（文字は無し、ホバーで tooltip「メモ」）。2. 同じ位置に幅 320px の入力欄（placeholder「メモ」）が出てフォーカスが当たり、アイコンは消える。3. 欄が閉じて toast「メモを保存しました」。`~/.config/mzed/notes/<UTC>-<8桁hex>.json` が1件でき、`heading` が選択位置の直前の見出し（`## 見出し` 形式）、`quote` が選択した本文、`rel_path` が project root 基準の相対パスになっている |
| **備考** | ファイル名は `ls` が時系列に並ぶ。`created_at` は UTC 秒精度 |

### NOTE-02 選択が無いと保存されず toast が出る

| 項目 | 内容 |
|---|---|
| **前提** | Markdown を開いていて、本文のどこかをクリックして選択を解いている |
| **手順** | Cmd+Shift+M を押す |
| **期待結果** | ポップオーバーは出ず、toast「本文を選択してからメモを追加してください」。`notes/` にファイルが増えない |
| **備考** | 本文をクリックして選択を解いた時点で、WebView 側が覚えている選択も忘れ、アイコンも消える |

### NOTE-03 本文の右クリックは常に標準メニュー

| 項目 | 内容 |
|---|---|
| **前提** | Markdown を開いている |
| **手順** | 1. 本文を選択して、その選択範囲の上で右クリックする。2. 選択を解いてから本文を右クリックする |
| **期待結果** | 1〜2. どちらも WebView 標準のメニュー（コピー / 調べる等）が出る。mzed 独自のメニューは出ない |
| **備考** | メモの入口はアイコン / Cmd+Shift+M / パレットの3つ。右クリックの項目は B-7 で削除した |

### NOTE-04 コマンドパレットから追加・保存先を開ける

| 項目 | 内容 |
|---|---|
| **前提** | Markdown を開いている |
| **手順** | 1. 本文を選択したまま Cmd+Shift+P → `Add Note` を実行し、入力して Enter。2. Cmd+Shift+P → `Open Notes Folder` を実行する |
| **期待結果** | 1. パレットが閉じて、選択終端にポップオーバーが出る（右上ではない）。引用はパレットを開く前に選択していた範囲になる（パレットの入力欄にフォーカスが移っても失われない）。2. Finder で `~/.config/mzed/notes/` が開く（無ければ作られる） |

### NOTE-05 空メモ・Esc では保存しない

| 項目 | 内容 |
|---|---|
| **前提** | 本文を選択して Cmd+Shift+M でポップオーバーを出している |
| **手順** | 1. 何も入力せず Enter。2. 空白だけ入力して Enter。3. Esc を押す。4. もう一度出して、文字を入力してから本文の別の場所をクリックする |
| **期待結果** | 1〜2. 何も起きない（欄は開いたまま、ファイルも増えない）。3. 欄が閉じて保存もされず、選択終端にアイコンが戻る。4. 欄が閉じて保存されない（外側クリックは破棄。× ボタンは無い） |
| **自動化済み** | 空メモの拒否は `src/services/notes.rs` の `空メモは保存しない`。UI 側の Enter 無効化は手動確認 |

### NOTE-06 左右分割で選択したペインのファイルに紐づく

| 項目 | 内容 |
|---|---|
| **前提** | Cmd+\ で左右分割し、左右に別のファイルを開いている |
| **手順** | 1. 右ペインの本文を選択して Cmd+Shift+M → 入力 → Enter。2. 左ペインでも同じ操作をする。3. 左ペインの先頭から右ペインの末尾までドラッグして選択し、Cmd+Shift+M |
| **期待結果** | 1〜2. それぞれのメモの `file` が、選択したペインで開いていたファイルになる（フォーカス中ペインではなく選択のあったペイン）。アイコンも選択したペインの中に出る。3. アイコンもポップオーバーも出ず toast「本文を選択してからメモを追加してください」（どちらのファイルのものか決まらないため） |

### NOTE-07 Task View のプレビューはメモ対象外

| 項目 | 内容 |
|---|---|
| **前提** | Cmd+Shift+D で Task View を開き、右ペインに task.md を表示している |
| **手順** | 右ペインの本文を選択して Cmd+Shift+M を押す |
| **期待結果** | アイコンは出ず、ポップオーバーも出ず toast「本文を選択してからメモを追加してください」 |
| **備考** | メモはペイン本文（`data-mdo-pane` を持つ `.markdown-body`）の選択だけを対象にする。Task View のプレビューはどのタブにも紐づかないため除外している |

### NOTE-08 `mzed serve` にはメモ機能が無い

| 項目 | 内容 |
|---|---|
| **前提** | `mzed serve ./docs` でブラウザ表示している |
| **手順** | 本文を選択して指を離す。Cmd+Shift+M を押す |
| **期待結果** | アイコンは出ない。何も起きない。右クリックはブラウザ標準のメニュー。保存先にファイルが増えない |

### NOTE-09 root 外のファイルのメモは親ディレクトリが root になる

| 項目 | 内容 |
|---|---|
| **前提** | プロジェクトを開いた状態で、その root 外の `.md` をウィンドウにドラッグ&ドロップして開く |
| **手順** | その本文を選択してメモを保存する |
| **期待結果** | `project_root` がそのファイルの親ディレクトリ、`rel_path` がファイル名だけになる |
| **自動化済み** | `src/services/notes.rs` の `root外のファイルは親ディレクトリをrootにする` |

### NOTE-10 メモ欄を開いた後にタブを切り替えても選択時のファイルに保存される

| 項目 | 内容 |
|---|---|
| **前提** | 同じプロジェクトで2つ以上の Markdown をタブで開いている |
| **手順** | 1. タブ A の本文を選択して Cmd+Shift+M。2. ポップオーバーを開いたままタブ B へ切り替える。3. ポップオーバーに入力して Enter。4. タブ A を閉じてから、別の選択で同じ手順を繰り返す |
| **期待結果** | 3. 保存された JSON の `file` / `rel_path` がタブ A のファイル（引用はタブ A の本文なので）。4. 閉じた後でも同じくタブ A のファイルに保存される |
| **備考** | 紐づけ先は選択取得時に確定する。この TC が落ちると引用と `file` が食い違う（再発検知用） |

### NOTE-11 再描画後に古い引用が残らない

| 項目 | 内容 |
|---|---|
| **前提** | Markdown を開いている |
| **手順** | 1. 本文を選択する。2. 選択したまま別タブへ切り替えて戻る（またはエディタでそのファイルを保存してライブリロードさせる、テーマを切り替える）。3. Cmd+Shift+M を押す |
| **期待結果** | 2. 再描画と同時にアイコンが消える。3. ポップオーバーは出ず toast「本文を選択してからメモを追加してください」。再描画前の引用が入った欄が開かない |
| **備考** | 覚えている選択は、引用元のテキストノードが document から外れた時点で破棄される（再描画の検知は `MutationObserver`） |
| **自動化済み** | JS トラッカーの不変条件は `src/js.rs` の `note_bridge_drops_a_remembered_selection_once_its_text_is_gone` 他。Node harness（`z-ai/2026-09-12-notes/harness.mjs`）の `re-render takes the icon with it`。実 WebView での再描画は手動確認 |

### NOTE-12 Cmd+F とメモ欄は同時に出ない

| 項目 | 内容 |
|---|---|
| **前提** | Markdown を開いている |
| **手順** | 1. 本文を選択して Cmd+Shift+M でポップオーバーを出す。2. そのまま Cmd+F を押す。3. 検索バーが出た状態で、本文を選択して Cmd+Shift+M を押す |
| **期待結果** | 2. ポップオーバーが閉じて検索バーだけになる。3. 検索バーが閉じてポップオーバーだけになる。どちらの向きでも2つが重ならない |

### NOTE-13 長すぎる選択・メモは切って保存する

| 項目 | 内容 |
|---|---|
| **前提** | 4,000 文字を超える本文（長いコードブロック等）を含む、十分に長い Markdown |
| **手順** | 4,000 文字を超える範囲をドラッグで選択して（本文全体の 90% 未満に収める）Cmd+Shift+M → 入力 → Enter |
| **期待結果** | toast「メモを保存しました（長すぎる部分は切りました）」。JSON の `quote` が 4,000 文字で切れている（`heading` は 300 文字、`note` は 4,000 文字が上限） |
| **備考** | 本文全体の 90% 以上を選ぶと NOTE-14 の対象外扱いになるので、長い文書で試す |
| **自動化済み** | `src/services/notes.rs` の `長すぎる引用と見出しとメモは上限で切られる` / `over_limitは上限を超えた入力だけを報告する` |

### NOTE-14 全体を選択した状態にはメモを付けられない

| 項目 | 内容 |
|---|---|
| **前提** | Markdown を開いている |
| **手順** | 1. 本文の先頭から末尾までドラッグで選択する（または Cmd+A）。2. Cmd+Shift+M を押す。3. 選択を一部に狭めて指を離す |
| **期待結果** | 1. アイコンが出ない。2. ポップオーバーは出ず toast「全体を選択した状態ではメモを付けられません」。`notes/` にファイルが増えない。3. アイコンが戻る |
| **備考** | 判定は「空白を除いた選択文字数がペイン本文の 90% 以上」。全体を直すなら CLI で直接直す |
| **自動化済み** | Node harness の `a whole-pane selection is reported as too broad` / `a whole-pane selection floats no icon` |

### NOTE-15 アイコンは選択に追従し、選択を解くと消える

| 項目 | 内容 |
|---|---|
| **前提** | 1 画面に収まらない長さの Markdown を開いている |
| **手順** | 1. 本文をドラッグで選択して指を離す。2. ドラッグ中（指を離す前）にアイコンが出ないことを見る。3. 選択したままホイールでスクロールする。4. Shift+→ で選択を広げる。5. ウィンドウ幅を変えて本文を折り返し直す。6. 本文の別の場所をクリックする |
| **期待結果** | 1. 選択終端の右上にアイコンが出る。2. ドラッグ中は出ない。3. アイコンは本文と一緒に動き、選択範囲の終端に張り付いたままになる。4. 広げている間はアイコンが消え、キーを離すと新しい終端に出る（古い位置に残らない）。5. リサイズ後もアイコンが選択終端に付いている。6. 選択が解けてアイコンが消える |
| **自動化済み** | Node harness の `changing the selection takes the old icon away` / `keyup floats the icon for the new selection` / `resize repositions the icon without rebuilding it` |

### NOTE-16 ポップオーバー表示中は引用範囲が凍結される

| 項目 | 内容 |
|---|---|
| **前提** | Markdown を開いている |
| **手順** | 1. 本文を選択してアイコンをクリックする。2. 入力欄に文字を打つ。3. 入力欄をドラッグして入力テキストを選択する。4. Esc で閉じる |
| **期待結果** | 1〜2. 入力欄にフォーカスが移って OS の選択色は消えるが、引用範囲には同じ色の薄い背景が残り、何にメモを付けているか見え続ける。テキスト自体は選択・コピーの状態が変わらない（DOM を書き換えていない）。3. 入力欄の中で選択しても本文のハイライトは 1mm も動かない。4. 背景が消えてアイコンが戻る |
| **自動化済み** | Node harness の `a new selection cannot move the frozen highlight` |

### NOTE-17 別ペインの再描画では選択を捨てない

| 項目 | 内容 |
|---|---|
| **前提** | Cmd+\ で左右分割し、左右に別のファイルを開いている |
| **手順** | 1. 左ペインの本文を選択してアイコンを出す。2. 右ペインのファイルをエディタで保存してライブリロードさせる（または右ペインのタブを切り替える） |
| **期待結果** | 2. 左ペインのアイコンは消えず、そのままクリックしてメモを保存できる（引用は左ペインのまま） |
| **自動化済み** | Node harness の `the other pane re-rendering keeps the icon` / `the other pane re-rendering keeps the selection` |

### NOTE-18 大きい文書でもドラッグ選択が詰まらず、見出しは直前のもの

| 項目 | 内容 |
|---|---|
| **前提** | 見出しが数千・本文が数 MB ある .md を表示している |
| **手順** | 1. 文書後半の段落をゆっくりドラッグで選択して指を離す。2. アイコンをクリックしてメモを保存する。3. 保存されたメモ JSON の `heading` を見る |
| **期待結果** | 1. ドラッグ中に選択の追従が引っかからない。指を離すとアイコンが出る。3. `heading` が選択開始位置の直前にある見出しになる |
| **備考** | `selectionchange` は 1 フレーム 1 回、本文の文字数と見出し一覧は DOM が変わるまで覚えている。見出しの結果は修正前と同じことを jsdom ハーネスで確認済み |

### NOTE-19 日本語入力の変換確定 Enter では保存しない

| 項目 | 内容 |
|---|---|
| **前提** | macOS 標準（または Google）日本語入力を使える。メモ欄を開いている |
| **手順** | 1. 「ひょうげんがわかりづらいかも」と打ち、スペースで変換する。2. Enter で変換を確定する。3. もう一度 Enter。4. 続けて「。」を確定してから「きさいすべきでは」を変換・確定し、Enter。5. 英字入力、ペースト（Cmd+V）、Undo（Cmd+Z）をそれぞれ試してから Enter |
| **期待結果** | 1. 入力欄の変換中の文字が崩れない（ローマ字が残らない、先頭に余計な文字が入らない）。2. 欄は閉じず、「表現がわかりづらいかも」が入ったまま。確定した文字が 2 重に入らない。3. 保存され、JSON の `note` が「表現がわかりづらいかも」。4. `note` が「。記載すべきでは」のとおり。5. 英字・ペースト・Undo は従来どおり欄に反映され、保存される `note` も欄の表示どおり |
| **備考** | 変換中の keydown / input と、確定直後の同じ値の input は全体の keydown ブリッジが止める（`src/js/keyboard.rs`）。jsdom ハーネス（`z-ai/2026-09-24-notes-ime/jsdom/ime.mjs`）で合成イベントの経路は確認済み。実 IME の挙動は目視。ガードの撤去条件の確認もこの手順で行う（仕様 10「日本語入力（IME）」） |

### NOTE-20 他の入力欄でも変換確定 Enter / Esc で決定・終了しない

| 項目 | 内容 |
|---|---|
| **前提** | 日本語入力を使える |
| **手順** | Cmd+F の検索バー、Cmd+P / Cmd+Shift+P、Cmd+O、Cmd+Shift+F、サイドバーのリネーム（F2 / Enter）、設定の文字入力欄それぞれで、かなを打って変換し、1. Enter で確定、2. 別の変換中に Esc で取り消す |
| **期待結果** | 1. 確定の Enter では検索移動・候補の決定・リネーム確定・除外ディレクトリの追加が走らず、文字が確定するだけ。次の Enter で決定する。2. Esc は変換の取り消しだけで、オーバーレイは閉じない。確定後の検索結果は確定した文字列で絞り込まれる |

### NOTE-21 受信箱のメモは引用箇所に破線の下線とホバー表示が出る

| 項目 | 内容 |
|---|---|
| **前提** | `~/.config/mzed/notes/` に、表示する .md についてのメモが 2 件以上ある（1 件は同じ段落に重ねる、1 件は本文に無い `quote`） |
| **手順** | 1. その .md を開く。2. 下線にマウスを乗せる。3. 重なった 2 件の下線に乗せる。4. テーマをライト / ダークで切り替える。5. 本文を選択してアイコン・ポップオーバーを出し、下線に乗せる。6. 下線にマウスを乗せてツールチップを出したまま、別の選択をして Cmd+Shift+M でポップオーバーを開く。7. リンクを含む文にメモを付けておき、下線の上でリンクをクリックする。8. 本文に連続した空白を含む行（コードブロックの `let  value = 1;` など）を引用したメモを置く |
| **期待結果** | 1. 引用の 1 行目に 2px の破線の下線が出る（本文の文字は変わらない、選択・コピーもそのまま）。本文に無い引用は描かれず、エラーも出ない。2. ツールチップにメモ本文と `MM/DD HH:mm`（ローカル時刻）。HTML タグを書いたメモもタグの文字のまま出る。行の上半分ではツールチップは出ない。3. 2 件が時刻順に並ぶ。4. 下線の色がライト `#bf8700` / ダーク `#d29922` に変わる。5. ポップオーバー表示中はツールチップを出さない。6. ポップオーバーが開いた時点でツールチップが消える。7. リンクが開く（下線がクリックを遮らない）。8. その行にも下線が出る |
| **備考** | 下線は本文の外の層（`.mdo-note-marks`）に重ねる。探し方は「見出しの節 → 本文全体」の最初の一致。jsdom ハーネス `z-ai/2026-09-24-notes-ime/jsdom/marks.mjs` で位置・textContent・性能（5MB + 50 件）を確認済み |

### NOTE-22 メモの追加・done/ への移動・ライブリロードで下線が追従する

| 項目 | 内容 |
|---|---|
| **前提** | NOTE-21 の状態 |
| **手順** | 1. 本文を選択してメモを保存する。2. 別ウィンドウ（またはエージェント）でメモ JSON を `done/` へ移す。3. 表示中の .md を外部で編集して保存する。4. 左右分割で右ペインにメモのある別ファイルを出す。5. メモのあるファイルとメモの無い大きいファイルのタブを素早く切り替える |
| **期待結果** | 1. 保存直後に新しい下線が出る。2. 移したメモの下線が消える。3. 再描画後も下線が正しい位置に出る。4. 右ペインの文書にもその文書のメモの下線が出る。5. 切り替え先に前のファイルのメモの下線が一瞬でも出ない |
| **備考** | 受信箱はウィンドウごとに 1 本の非再帰 watcher で見る。1〜3 は別名バンドル + HOME 分離で画面確認済み（2026-09-24） |

## タブバー（TAB）

### TAB-01 タブが増えても見切れず重ならない

| 項目 | 内容 |
|---|---|
| **前提** | プロジェクトを開いている |
| **手順** | 1. Cmd+P から 10 個以上の Markdown を次々に開く。2. タブバーを左端から右端まで横スクロールして全部見る。3. タブをバー幅に収まる数まで閉じ、もう一度溢れるまで開く |
| **期待結果** | 1. どのタブも途中で切れて描かれない。アクティブタブが他のタブに重なって被ることもない。長いファイル名は `…` で省略される。2. 横スクロールで全タブに到達でき、タブバー下端に 4px の細いスクロールバーが出る。3. スクロールバーが出入りしてもタブの文字が縦に切れない（`::-webkit-scrollbar` を指定した WKWebView ではスクロールバーが場所を取るため、バーの高さが 4px 分ずれる可能性がある。ずれが目に付くなら `.mdo-tabbar` に `padding-bottom` で場所を常に確保する） |

### TAB-02 ホイールとトラックパッドでタブバーを横送りできる

| 項目 | 内容 |
|---|---|
| **前提** | タブがバー幅を超えて並んでいる |
| **手順** | 1. タブバーの上でマウスの縦ホイールを回す。2. タブバーの上でトラックパッドの横スワイプをする |
| **期待結果** | 1〜2. どちらでもタブバーが横に送られる。本文のスクロール位置は動かない |

### TAB-03 タブを切り替えるとアクティブタブが可視範囲に寄る

| 項目 | 内容 |
|---|---|
| **前提** | タブがバー幅を超えて並んでいる |
| **手順** | 1. タブバーを右端までスクロールする。2. Ctrl+Shift+Tab でタブを左方向へ次々に切り替える。3. Cmd+P で左端のタブのファイルを開き直す |
| **期待結果** | 2〜3. アクティブになったタブが必ず見える位置までタブバーが自動でスクロールする（画面外に隠れたまま切り替わらない） |

### TAB-04 新しいタブが左端に入り、開き直しでは順序が変わらない

| 項目 | 内容 |
|---|---|
| **前提** | `tab_insert` が既定（`start`） |
| **手順** | 1. `a.md` → `b.md` → `c.md` の順に開く。2. サイドバーで `a.md` をもう一度クリックする |
| **期待結果** | 1. タブは左から `c.md` `b.md` `a.md` の順で、`c.md` がアクティブ。2. 並びは変わらず `a.md` がアクティブになるだけ |
| **自動化済み** | `src/tabs.rs` の `startなら新規タブが先頭に入る` / `start設定でも既存タブの再openは順序を変えない` |

### TAB-05 `tab_insert: "end"` では従来どおり末尾に追加される

| 項目 | 内容 |
|---|---|
| **前提** | 設定 General の「新しいタブの位置」を「右端」にする |
| **手順** | 1. `a.md` → `b.md` → `c.md` の順に開く。2. mzed を再起動する |
| **期待結果** | 1. タブは左から `a.md` `b.md` `c.md` の順。2. 再起動後も保存した順（左から `a.md` `b.md` `c.md`）で復元される（左端挿入設定でも復元順は反転しない） |
| **自動化済み** | `src/tabs.rs` の `openはタブを追加しアクティブにする`、`src/session.rs` の `復元は通常もプロジェクト別も保存順とアクティブを保つ`（実ファイル4件で通常復元・プロジェクト別復元の順序とアクティブを固定）。設定 select から `end` に切り替える操作自体は手動 |

### TAB-06 同名ファイルのタブには親ディレクトリ名が添えられる

| 項目 | 内容 |
|---|---|
| **前提** | 別フォルダに同名の `README.md` が 2 つあるプロジェクト |
| **手順** | 1. 両方の `README.md` を開く。2. タブにマウスを載せる |
| **期待結果** | 1. どちらのタブも `README.md` の後ろに親ディレクトリ名が薄く添えられ、区別できる。2. ツールチップにプロジェクトルートからの相対パスが出る |
